/* Spaced-repetition deck for Karl Stroetmann's "Formal Languages and Their
   Applications". Notation follows the lecture notes exactly:
     - the EMPTY STRING is  $\lambda$  (not \epsilon)
     - $\epsilon$ is the REGULAR EXPRESSION whose language is {lambda}
     - a DFA transition may return  $\Omega$  (the automaton "dies")
     - the extended transition function on strings is  $\delta^*$
     - equivalence of regular expressions is written  r_1 = r_2  (his "doteq")
     - the pumping lemma splits  s = uvw  and pumps  u v^h w
   kind: 'recall' = definition/tuple, 'apply' = compute, 'why' = understanding. */

export const cards = [

  /* ---------------- u1 : alphabet ---------------------------------------- */
  { id: 'c-alphabet-def', concept: 'alphabet', kind: 'recall',
    front: 'Definition: an **alphabet** $\\Sigma$.',
    back: 'A **finite, non-empty** set of characters, $\\Sigma$ = {c_1, \\ldots, c_n}. Finiteness is the point: everything later counts on being able to enumerate $\\Sigma$.' },
  { id: 'c-alphabet-string', concept: 'alphabet', kind: 'recall',
    front: 'What is a **string** over $\\Sigma$, and how is the empty string written?',
    back: 'A finite sequence of characters from $\\Sigma$, written without commas: $w = c_1 \\cdots c_n$. The empty string is $\\lambda$ — in these notes never $\\epsilon$.' },
  { id: 'c-alphabet-sigmastar', concept: 'alphabet', kind: 'recall',
    front: 'What is $\\Sigma^*$?',
    back: 'The set of **all** strings that can be formed from $\\Sigma$, including the empty string $\\lambda$.' },
  { id: 'c-alphabet-lang', concept: 'alphabet', kind: 'recall',
    front: 'Definition: a **formal language** over $\\Sigma$.',
    back: 'Any subset $L \\subseteq \\Sigma^*$ that is *precisely defined* — for every $w \\in \\Sigma^*$ it must be unambiguous whether $w \\in L$. English fails that test.' },
  { id: 'c-alphabet-index', concept: 'alphabet', kind: 'apply',
    front: 'For $w$ = `"example"`, what are $|w|$ and `w[0]`?',
    back: '$|w| = 7$ and `w[0]` = `e`. Indexing starts at 0, as in C, Java and Python.' },
  { id: 'c-alphabet-count', concept: 'alphabet', kind: 'why',
    front: 'Why is $\\Sigma$ required to be finite, but a language may be infinite?',
    back: '$\\Sigma$ is the fixed input vocabulary a machine must be able to distinguish; the language is the (possibly infinite) set of accepted inputs. The whole course is about describing infinite sets with finite text.' },

  /* ---------------- u1 : concat ------------------------------------------ */
  { id: 'c-concat-product', concept: 'concat', kind: 'recall',
    front: 'Definition: the **product** $L_1 \\cdot L_2$ of two languages.',
    back: '$L_1 \\cdot L_2$ := {w_1 w_2 | w_1 $\\in$ L_1 $\\land$ w_2 $\\in$ L_2}: every first-word glued to every second-word.' },
  { id: 'c-concat-power', concept: 'concat', kind: 'recall',
    front: 'Definition: the $n$-th power $L^n$ of a language.',
    back: 'Inductively: $L^{0}$ := {$\\lambda$} and $L^{n+1}$ := $L^{n} \\cdot L$. Note $L^{0}$ is **not** the empty set — it is the set containing the empty string.' },
  { id: 'c-concat-kleene', concept: 'concat', kind: 'recall',
    front: 'Definition: the **Kleene closure** $L^*$.',
    back: '$L^* := \\bigcup$ {$L^{n}$ | n $\\in$ N} = $L^{0} \\cup L^{1} \\cup L^{2} \\cup \\ldots$ Because $L^{0}$ = {$\\lambda$}, we always have $\\lambda \\in L^*$.' },
  { id: 'c-concat-emptystar', concept: 'concat', kind: 'apply',
    front: 'What is $L^*$ when $L$ = {} (the empty language)?',
    back: '{$\\lambda$}. Only the $n=0$ term survives, and $L^{0}$ = {$\\lambda$}. So $L^*$ is never empty.' },
  { id: 'c-concat-infinite', concept: 'concat', kind: 'why',
    front: 'When is $L^*$ infinite for a finite $L$?',
    back: 'As soon as $L$ contains one string other than $\\lambda$: that string can be repeated arbitrarily often, so $L^*$ has strings of unbounded length.' },
  { id: 'c-concat-example', concept: 'concat', kind: 'apply',
    front: '$L_1$ = {ab, bc}, $L_2$ = {ac, cb}. Compute $L_1 \\cdot L_2$.',
    back: '{abac, abcb, bcac, bccb} — all four pairings, in that order of gluing. $|L_1 \\cdot L_2| \\le |L_1| \\cdot |L_2|$, with equality when no two products collide.' },

  /* ---------------- u1 : regex-syntax ------------------------------------ */
  { id: 'c-rex-inductive', concept: 'regex-syntax', kind: 'recall',
    front: 'List the clauses that define the set `RegExp_Sigma`.',
    back: '$\\emptyset$; $\\epsilon$; every $c \\in \\Sigma$; and inductively $r_1 + r_2$, $r_1 \\cdot r_2$, $r^*$, and $(r)$. Seven clauses, no more.' },
  { id: 'c-rex-notinsigma', concept: 'regex-syntax', kind: 'why',
    front: 'Why must $\\emptyset$, $\\epsilon$, `+`, `.`, `*`, `(` and `)` not be elements of $\\Sigma$?',
    back: 'Otherwise a regular expression could not be read unambiguously — you could not tell a metacharacter from a literal character of the alphabet.' },
  { id: 'c-rex-precedence', concept: 'regex-syntax', kind: 'recall',
    front: 'State the operator precedences for regular expressions.',
    back: '$^*$ binds tightest, then $\\cdot$, then $+$ loosest. So $a + b \\cdot c^*$ means $a + (b \\cdot (c^*))$.' },
  { id: 'c-rex-parse', concept: 'regex-syntax', kind: 'apply',
    front: 'Fully parenthesise $a \\cdot b + c^*$.',
    back: '$((a \\cdot b) + (c^*))$. Star first, concatenation next, plus last.' },
  { id: 'c-rex-epsilon-vs-lambda', concept: 'regex-syntax', kind: 'why',
    front: 'What is the difference between $\\epsilon$ and $\\lambda$ in these notes?',
    back: '$\\lambda$ is the empty **string**, an element of $\\Sigma^*$. $\\epsilon$ is a **regular expression**, a piece of syntax, with $L(\\epsilon)$ = {$\\lambda$}. Confusing them collapses syntax and semantics.' },
  { id: 'c-rex-emptyvs', concept: 'regex-syntax', kind: 'why',
    front: 'Why are $\\emptyset$ and $\\epsilon$ two different regular expressions?',
    back: '$L(\\emptyset)$ = {} has **no** words; $L(\\epsilon)$ = {$\\lambda$} has exactly **one**, of length 0. A set with one element is not an empty set.' },

  /* ---------------- u1 : regex-sem --------------------------------------- */
  { id: 'c-sem-signature', concept: 'regex-sem', kind: 'recall',
    front: 'What is the signature of the semantic function $L$?',
    back: '$L$: `RegExp_Sigma` $\\to$ $2^{\\Sigma^*}$ — it maps each regular expression to a **subset** of $\\Sigma^*$, i.e. to an element of the power set.' },
  { id: 'c-sem-base', concept: 'regex-sem', kind: 'recall',
    front: 'Give the three base cases of $L(\\cdot)$.',
    back: '$L(\\emptyset)$ = {}, $L(\\epsilon)$ = {$\\lambda$}, and $L(c)$ = {c} for $c \\in \\Sigma$ (a character is identified with the string of length one).' },
  { id: 'c-sem-step', concept: 'regex-sem', kind: 'recall',
    front: 'Give the three inductive cases of $L(\\cdot)$.',
    back: '$L(r_1 + r_2)$ = $L(r_1) \\cup L(r_2)$; $L(r_1 \\cdot r_2)$ = $L(r_1) \\cdot L(r_2)$; $L(r^*)$ = $(L(r))^*$. Syntax operators map to language operators one-for-one.' },
  { id: 'c-sem-len2', concept: 'regex-sem', kind: 'apply',
    front: 'With $\\Sigma$ = {a,b,c}, describe $L((a+b+c) \\cdot (a+b+c))$ in words.',
    back: 'All strings over $\\Sigma$ of length exactly 2. Concatenating two "any character" choices forces exactly two characters.' },
  { id: 'c-sem-onea', concept: 'regex-sem', kind: 'apply',
    front: 'What language does $(b+c)^* \\cdot a \\cdot (b+c)^*$ denote over {a,b,c}?',
    back: 'All strings containing **exactly one** `a`. The two starred blocks can produce only b and c, so the single `a` in the middle is the only one.' },
  { id: 'c-sem-nosubstring', concept: 'regex-sem', kind: 'apply',
    front: 'Over {0,1}, what does $(1+\\epsilon) \\cdot (0 \\cdot 0^* \\cdot 1)^* \\cdot 0^*$ describe?',
    back: 'All strings that do **not** contain the substring `11`: every 1 produced inside the star is preceded by at least one 0, and only a single leading 1 is allowed.' },

  /* ---------------- u1 : regex-algebra ----------------------------------- */
  { id: 'c-alg-doteq', concept: 'regex-algebra', kind: 'recall',
    front: 'How is equivalence of two regular expressions defined?',
    back: '$r_1$ is equivalent to $r_2$ (his notation: $r_1 \\doteq r_2$) **iff** $L(r_1)$ = $L(r_2)$. Equivalence is about languages, never about the shape of the expression.' },
  { id: 'c-alg-idem', concept: 'regex-algebra', kind: 'recall',
    front: 'Why is $r + r \\doteq r$?',
    back: 'Because $L(r+r)$ = $L(r) \\cup L(r)$ = $L(r)$ — union is idempotent on sets.' },
  { id: 'c-alg-emptyabsorb', concept: 'regex-algebra', kind: 'recall',
    front: 'Simplify $\\emptyset \\cdot r$ (and $r \\cdot \\emptyset$).',
    back: '$\\emptyset \\cdot r \\doteq r \\cdot \\emptyset \\doteq \\emptyset$ — there is nothing to glue, so the product language is empty.' },
  { id: 'c-alg-emptyneutral', concept: 'regex-algebra', kind: 'recall',
    front: 'Simplify $\\emptyset + r$ (and $r + \\emptyset$).',
    back: '$\\emptyset + r \\doteq r + \\emptyset \\doteq r$ — $\\emptyset$ is the neutral element of $+$, since it contributes no words to the union.' },
  { id: 'c-alg-epsneutral', concept: 'regex-algebra', kind: 'recall',
    front: 'Simplify $\\epsilon \\cdot r$.',
    back: '$\\epsilon \\cdot r \\doteq r$: $\\epsilon$ is the neutral element of concatenation, since gluing $\\lambda$ in front changes nothing.' },
  { id: 'c-alg-emptystar', concept: 'regex-algebra', kind: 'apply',
    front: 'Simplify $\\emptyset^*$ and $\\epsilon^*$.',
    back: 'Both simplify to $\\epsilon$. $L(\\emptyset^*)$ = {}$^*$ = {$\\lambda$} and $L(\\epsilon^*)$ = {$\\lambda$}$^*$ = {$\\lambda$}.' },
  { id: 'c-alg-starstar', concept: 'regex-algebra', kind: 'why',
    front: 'Why is $(r^*)^* \\doteq r^*$?',
    back: 'A concatenation of concatenations of $r$-words is still a concatenation of $r$-words, so the outer star adds nothing. Formally $L((r^*)^*) \\subseteq L(r^*)$ by flattening, and $\\subseteq$ the other way is trivial.' },
  { id: 'c-alg-unroll', concept: 'regex-algebra', kind: 'apply',
    front: 'Simplify $\\varepsilon + r^* \\cdot r$.',
    back: '$\\varepsilon + r^* \\cdot r \\doteq r^*$ — the unrolling law: either zero repetitions, or some repetitions followed by one more.' },

  { id: 'c-alg-staropt', concept: 'regex-algebra', kind: 'recall',
    front: 'Simplify $(\\varepsilon + r)^*$.',
    back: '$(\\varepsilon + r)^* \\doteq r^*$ — adding the option of "nothing" underneath a star is free, because $r^*$ already contains $\\lambda$.' },
  /* ---------------- u2 : scanner ----------------------------------------- */
  { id: 'c-scan-job', concept: 'scanner', kind: 'recall',
    front: 'What is the job of a **scanner**?',
    back: 'To split the character stream into a list of **tokens**, discarding whitespace and comments. In these notes a token is a `Token` object with the four attributes `type`, `value`, `line`, `column`. Every compiler starts by running a scanner.' },
  { id: 'c-scan-order', concept: 'scanner', kind: 'why',
    front: 'Why does the **order** of the pairs in a `re`-based token specification matter?',
    back: 'Python\'s `re` module takes the **first** alternative (left to right) that matches at the current position, not the longest one. So specific patterns (keywords) must be listed before general ones (identifiers).' },
  { id: 'c-scan-findall', concept: 'scanner', kind: 'apply',
    front: 'Why does `re.findall(r`[0-9]|[1-9][0-9]*`, "42")` return two single digits?',
    back: 'The first alternative `[0-9]` already matches `4`, so matching restarts after it. Swapping the alternatives gives `42`. First-match, not longest-match.' },
  { id: 'c-scan-master', concept: 'scanner', kind: 'recall',
    front: 'What is the **master pattern** of a scanner built with `re`?',
    back: 'All token regexes wrapped in named groups `(?P<NAME>regex)` and joined with `|`. After a match, `lastgroup` says which token class fired.' },
  { id: 'c-scan-bookkeeping', concept: 'scanner', kind: 'recall',
    front: 'What are the pseudo-tokens `NEWLINE`, `SKIP` and `MISMATCH` for?',
    back: '`NEWLINE` bumps the line counter, `SKIP` swallows blanks and tabs, and `MISMATCH` matches any remaining single character so an illegal character is reported instead of silently skipped.' },
  { id: 'c-scan-state', concept: 'scanner', kind: 'why',
    front: 'Why can `re.finditer` not be used once a scanner has **states**?',
    back: '`finditer` fixes the pattern for the whole scan. A stateful scanner has to change the pattern between tokens, so it must call `match` at an explicit position in a loop instead.' },

  /* ---------------- u2 : dfa --------------------------------------------- */
  { id: 'c-dfa-tuple', concept: 'dfa', kind: 'recall',
    front: 'Give the 5-tuple of a **DFA**.',
    back: '$F$ = $\\langle Q, \\Sigma, \\delta, q_0, A \\rangle$: finite state set $Q$, input alphabet $\\Sigma$, transition function $\\delta$, start state $q_0 \\in Q$, accepting states $A \\subseteq Q$.' },
  { id: 'c-dfa-delta', concept: 'dfa', kind: 'recall',
    front: 'What is the exact signature of $\\delta$ in Stroetmann\'s DFA definition?',
    back: '$\\delta$: $Q \\times \\Sigma \\to Q \\cup$ {$\\Omega$}. The extra value $\\Omega$ means the automaton **dies** — the transition is undefined.' },
  { id: 'c-dfa-die', concept: 'dfa', kind: 'recall',
    front: 'What does it mean that a DFA **dies**?',
    back: 'It reads a character $c$ in a state $q$ with $\\delta(q,c) = \\Omega$. There is no successor state, so the input is rejected. In the drawings such transitions are simply not shown.' },
  { id: 'c-dfa-complete', concept: 'dfa', kind: 'recall',
    front: 'When is a DFA **complete**, and how do you complete one?',
    back: 'Complete iff $\\delta(q,c) \\neq \\Omega$ for all $q, c$. Add a **dead state** (the skull), send every undefined transition there, and loop it to itself on every character. The language does not change, because the dead state is not accepting.' },
  { id: 'c-dfa-accept', concept: 'dfa', kind: 'recall',
    front: 'Definition of $L(F)$ for a DFA $F$.',
    back: '$L(F)$ := {s $\\in \\Sigma^*$ | $\\delta^*(q_0, s) \\in A$} — the strings that carry $F$ from the start state into an accepting state.' },
  { id: 'c-dfa-count', concept: 'dfa', kind: 'apply',
    front: 'How many states does a DFA need for $(a+b)^* \\cdot b \\cdot (a+b) \\cdot (a+b)$ over {a,b}?',
    back: '8. It must remember the last three characters read, and $2^{3}$ = 8. Antepenultimate-character languages cost exponentially in the look-back distance.' },

  /* ---------------- u2 : dfa-run ----------------------------------------- */
  { id: 'c-run-deltastar-base', concept: 'dfa-run', kind: 'recall',
    front: 'Define $\\delta^*(q, w)$ by induction on $w$.',
    back: 'Base: $\\delta^*(q, \\lambda)$ := $q$. Step: $\\delta^*(q, cv)$ := $\\delta^*(\\delta(q,c), v)$ if $\\delta(q,c) \\neq \\Omega$, otherwise $\\Omega$. Death propagates.' },
  { id: 'c-run-lambda', concept: 'dfa-run', kind: 'why',
    front: 'Why is $\\delta^*(q, \\lambda) = q$ and not something else?',
    back: 'Because a deterministic automaton changes state only by reading a character. Reading nothing changes nothing. This is why $\\lambda \\in L(F)$ iff $q_0 \\in A$.' },
  { id: 'c-run-trace', concept: 'dfa-run', kind: 'apply',
    front: 'DFA: $Q$={0,1}, $\\delta(0,a)$=0, $\\delta(0,b)$=1, $\\delta(1,a)$=1, $\\delta(1,b)$=$\\Omega$, $q_0$=0, $A$={1}. Is `aabab` accepted?',
    back: 'No. Trace 0 -a-> 0 -a-> 0 -b-> 1 -a-> 1 -b-> $\\Omega$: it dies on the second `b`. The language is $a^* \\cdot b \\cdot a^*$ — exactly one `b`.' },
  { id: 'c-run-empty-accept', concept: 'dfa-run', kind: 'apply',
    front: 'Same DFA ($L$ = $a^* b a^*$). Is $\\lambda$ accepted?',
    back: 'No: $\\delta^*(0,\\lambda)$ = 0 and 0 $\\notin$ A. Every word of the language contains one `b`.' },
  { id: 'c-run-reach', concept: 'dfa-run', kind: 'recall',
    front: 'When is a state $p$ **reachable**?',
    back: 'When some $w \\in \\Sigma^*$ has $\\delta^*(q_0, w) = p$. Unreachable states are useless and are discarded before minimisation.' },

  /* ---------------- u2 : nfa --------------------------------------------- */
  { id: 'c-nfa-tuple', concept: 'nfa', kind: 'recall',
    front: 'Give the signature of $\\delta$ for an **NFA**.',
    back: '$\\delta$: $Q \\times (\\Sigma \\cup$ {$\\epsilon$}$) \\to 2^{Q}$. It returns a **set** of states, and the extra input $\\epsilon$ allows moves that consume no character.' },
  { id: 'c-nfa-accept', concept: 'nfa', kind: 'recall',
    front: 'Define $L(F)$ for an NFA via configurations.',
    back: 'A configuration is a pair $\\langle q, s \\rangle$. $L(F)$ := {s | $\\exists p \\in A$: $\\langle q_0, s\\rangle \\leadsto^* \\langle p, \\lambda\\rangle$} — **some** path consumes all input and lands in an accepting state.' },
  { id: 'c-nfa-config', concept: 'nfa', kind: 'recall',
    front: 'What are the two rules of the configuration relation $\\leadsto$?',
    back: '$\\langle q_1, cs\\rangle \\leadsto \\langle q_2, s\\rangle$ if $q_2 \\in \\delta(q_1, c)$ (a character is consumed), and $\\langle q_1, s\\rangle \\leadsto \\langle q_2, s\\rangle$ if $q_2 \\in \\delta(q_1, \\epsilon)$ (nothing is consumed).' },
  { id: 'c-nfa-dies', concept: 'nfa', kind: 'why',
    front: 'An NFA "guesses wrongly" and dies. Is the string rejected?',
    back: 'Not necessarily. Rejection requires that **every** computation fails. One bad guess proves nothing; acceptance is existential over paths.' },
  { id: 'c-nfa-savings', concept: 'nfa', kind: 'apply',
    front: 'How many states does the NFA for $(a+b)^* b (a+b)(a+b)$ need, versus the DFA?',
    back: '4 versus 8. The NFA guesses *which* `b` is the antepenultimate character instead of remembering the whole window.' },
  { id: 'c-nfa-power', concept: 'nfa', kind: 'why',
    front: 'Are NFAs more powerful than DFAs?',
    back: 'No — they are equally powerful. The subset construction turns any NFA into a DFA for the same language. NFAs buy **conciseness**, not expressiveness.' },

  /* ---------------- u2 : eclosure ---------------------------------------- */
  { id: 'c-ec-def', concept: 'eclosure', kind: 'recall',
    front: 'Define `ec(q)`, the $\\epsilon$-closure of a state.',
    back: 'Inductively: $q \\in$ `ec(q)`; and if $p \\in$ `ec(q)` and $r \\in \\delta(p, \\epsilon)$ then $r \\in$ `ec(q)`. The smallest such set — a least fixed point.' },
  { id: 'c-ec-self', concept: 'eclosure', kind: 'why',
    front: 'Why does `ec(q)` always contain $q$ itself?',
    back: 'Zero $\\epsilon$-moves is a legal number of $\\epsilon$-moves; the base clause makes the closure reflexive. Forgetting this loses the state you started in.' },
  { id: 'c-ec-vs-reach', concept: 'eclosure', kind: 'why',
    front: 'How does the $\\epsilon$-closure differ from plain reachability?',
    back: 'Reachability follows **all** transitions; `ec` follows only the $\\epsilon$-labelled ones. A state reachable by reading `a` is not in the closure.' },
  { id: 'c-ec-deltahat', concept: 'eclosure', kind: 'recall',
    front: 'Define the extended transition $\\hat\\delta(q_1, c)$ used in the subset construction.',
    back: '$\\hat\\delta(q_1,c)$ := $\\bigcup$ {`ec(q_2)` | $q_2 \\in \\delta(q_1,c)$}. **First** read the character, **then** take $\\epsilon$-closures.' },
  { id: 'c-ec-order', concept: 'eclosure', kind: 'why',
    front: 'Why does $\\hat\\delta$ close under $\\epsilon$ only *after* the character is read?',
    back: 'It is a convention of the definition: $\\widehat\\delta(q_1,c) := \\bigcup$ {`ec(q_2)` | $q_2 \\in \\delta(q_1,c)$}. The $\\varepsilon$-moves before the first character are covered instead by the start state `ec(q_0)` of `det(F)`.' },
  { id: 'c-ec-compute', concept: 'eclosure', kind: 'apply',
    front: 'NFA with $q_5 \\to^\\epsilon q_7$, $q_7 \\to^\\epsilon q_0$, $q_0 \\to^\\epsilon q_1$, $q_0 \\to^\\epsilon q_2$. What is `ec(q_5)`?',
    back: '{q_5, q_7, q_0, q_1, q_2}. Follow $\\epsilon$-arrows transitively until nothing new appears.' },

  /* ---------------- u2 : thompson ---------------------------------------- */
  { id: 'c-th-invariant', concept: 'thompson', kind: 'recall',
    front: 'Which two properties does every NFA $A(r)$ built from a regular expression $r$ satisfy?',
    back: '(1) no transition **into** the start state; (2) exactly **one** accepting state, with no transition **out** of it. These invariants are what make the composition rules glue safely.' },
  { id: 'c-th-base', concept: 'thompson', kind: 'recall',
    front: 'Describe $A(\\emptyset)$, $A(\\epsilon)$ and $A(c)$.',
    back: 'All three have two states $q_0, q_1$. $A(\\emptyset)$ has **no** transition, $A(\\epsilon)$ has $q_0 \\to^\\epsilon q_1$, and $A(c)$ has $q_0 \\to^{c} q_1$.' },
  { id: 'c-th-concat', concept: 'thompson', kind: 'recall',
    front: 'How is $A(r_1 \\cdot r_2)$ built?',
    back: 'Take the two fragments disjointly and add one $\\epsilon$-transition from the accepting state of $A(r_1)$ to the start state of $A(r_2)$. Start = start of $A(r_1)$, accept = accept of $A(r_2)$.' },
  { id: 'c-th-plus', concept: 'thompson', kind: 'recall',
    front: 'How is $A(r_1 + r_2)$ built?',
    back: 'Add a new start $q_0$ with $\\epsilon$-transitions to both fragment starts, and a new accepting state $q_5$ reached by $\\epsilon$ from both fragment accepting states. Four new $\\epsilon$-edges, two new states.' },
  { id: 'c-th-star', concept: 'thompson', kind: 'recall',
    front: 'How is $A(r^*)$ built from $A(r)$ = $\\langle Q,\\Sigma,\\delta,q_1,\\{q_2\\}\\rangle$?',
    back: 'New states $q_0$ (start) and $q_3$ (accept), with $\\epsilon$-edges $q_0 \\to$ {q_1, q_3} and $q_2 \\to$ {q_1, q_3}: skip the body, or run it and loop back.' },
  { id: 'c-th-nomerge', concept: 'thompson', kind: 'why',
    front: 'Why must you **not** merge $q_0$ with $q_1$ and $q_2$ with $q_3$ in $A(r^*)$?',
    back: 'The notes flag this in red: merging lets the automaton re-enter the body from the exit and accept strings that are not in $L(r^*)$. For concatenation, by contrast, merging the two touching states is safe.' },

  /* ---------------- u2 : subset ------------------------------------------ */
  { id: 'c-sub-idea', concept: 'subset', kind: 'recall',
    front: 'What is the central idea of the subset construction `det(F)`?',
    back: 'A state of `det(F)` is the **set of all NFA states** the NFA could be in after reading the input so far. Determinism is bought by tracking every possibility at once.' },
  { id: 'c-sub-tuple', concept: 'subset', kind: 'recall',
    front: 'Write down `det(F)` as a 5-tuple.',
    back: '`det(F)` = $\\langle 2^{Q}, \\Sigma, \\Delta,$ `ec(q_0)`$, \\hat{A} \\rangle$ with $\\hat{A}$ = {M | M $\\cap$ A $\\neq$ {}}.' },
  { id: 'c-sub-delta-big', concept: 'subset', kind: 'recall',
    front: 'Define $\\Delta(M, c)$ for a set $M$ of NFA states.',
    back: '$\\Delta(M,c)$ := $\\bigcup$ {$\\hat\\delta(q,c)$ | $q \\in M$} — take every state in $M$, read $c$, follow $\\epsilon$-moves, union it all up.' },
  { id: 'c-sub-start', concept: 'subset', kind: 'why',
    front: 'Why is the start state of `det(F)` equal to `ec(q_0)` and not {q_0}?',
    back: 'Before any character is read the NFA may already have slipped along $\\epsilon$-edges, so all of those states are live from the outset.' },
  { id: 'c-sub-accept', concept: 'subset', kind: 'why',
    front: 'Why is a set $M$ accepting iff $M \\cap A \\neq$ {}?',
    back: 'The NFA accepts if **some** path ends accepting; $M$ records all reachable end states, so one accepting member suffices.' },
  { id: 'c-sub-blowup', concept: 'subset', kind: 'why',
    front: 'Why does the subset construction terminate, and what is the worst-case state count?',
    back: 'There are only finitely many subsets of the finite $Q$, at most $2^{|Q|}$, so the search for reachable sets must stop. In practice most of these subsets are unreachable and are not needed as states.' },
  { id: 'c-sub-count', concept: 'subset', kind: 'apply',
    front: 'The 4-state NFA for $(a+b)^* b(a+b)(a+b)$ gives a DFA with 8, not 16, states. Why?',
    back: 'State 0 has an $a$- and a $b$-transition back to itself, so **every** reachable set contains 0. Only the 8 subsets containing 0 occur.' },

  /* ---------------- u2 : minimize ---------------------------------------- */
  { id: 'c-min-separable', concept: 'minimize', kind: 'recall',
    front: 'Definition: two states $p_1, p_2$ are **separable**.',
    back: 'There is a string $s$ with $\\delta^*(p_1,s) \\in A$ and $\\delta^*(p_2,s) \\notin A$, or vice versa. Such a $s$ **separates** them.' },
  { id: 'c-min-equiv', concept: 'minimize', kind: 'recall',
    front: 'How is the equivalence $p_1 \\sim p_2$ defined?',
    back: 'As "not separable": $\\forall s \\in \\Sigma^*$: $\\delta^*(p_1,s) \\in A \\leftrightarrow \\delta^*(p_2,s) \\in A$. The minimal DFA has the classes $[q]_\\sim$ as its states.' },
  { id: 'c-min-prereq', concept: 'minimize', kind: 'recall',
    front: 'Which two preconditions does the minimisation algorithm assume?',
    back: 'The DFA is **complete** ($\\delta$ never returns $\\Omega$) and every state is **reachable** from $q_0$. Unreachable states are deleted first.' },
  { id: 'c-min-iterate', concept: 'minimize', kind: 'recall',
    front: 'How is the set $V$ of separable pairs built up?',
    back: 'Start with all pairs $\\langle p,q \\rangle$ where exactly one of $p,q$ is accepting. Then add $\\langle p,q\\rangle$ whenever some $c$ has $\\langle\\delta(p,c), \\delta(q,c)\\rangle \\in V$. Iterate to a fixed point.' },
  { id: 'c-min-why-first', concept: 'minimize', kind: 'why',
    front: 'Why are "accepting vs non-accepting" the first separable pairs?',
    back: 'They are separated by $s = \\lambda$: reading nothing, one is already accepting and the other is not. Everything else is discovered by pushing this backwards through $\\delta$.' },
  { id: 'c-min-fixpoint', concept: 'minimize', kind: 'why',
    front: 'Why does the separability iteration terminate, and why is the result minimal?',
    back: '$V$ only grows and is bounded by the finitely many pairs of states. At the fixed point no string distinguishes the remaining pairs, so merging them cannot change $L(F)$ — and no smaller DFA exists.' },

  /* ---------------- u3 : closure-props ----------------------------------- */
  { id: 'c-cl-list', concept: 'closure-props', kind: 'recall',
    front: 'Under which operations are regular languages closed?',
    back: 'The Boolean operations: **union**, **intersection**, **complement** $\\Sigma^* \\setminus L$ and, as a corollary, **set difference** $L_1 \\setminus L_2$. (Reversal $L^R$ is left as an exercise.)' },
  { id: 'c-cl-union', concept: 'closure-props', kind: 'why',
    front: 'Why is closure under **union** the easiest of the proofs?',
    back: 'It is immediate from the syntax: if $L_i = L(r_i)$ then $L_1 \\cup L_2 = L(r_1 + r_2)$. No automaton is needed at all.' },
  { id: 'c-cl-complement', concept: 'closure-props', kind: 'recall',
    front: 'How do you build a DFA for $\\Sigma^* \\setminus L$?',
    back: 'Take a **complete, deterministic** automaton for $L$ and swap the accepting states: $\\overline{\\mathtt{det}(F)}$ = $\\langle Q, \\Sigma, \\delta, q_0, Q \\setminus A\\rangle$.' },
  { id: 'c-cl-complement-trap', concept: 'closure-props', kind: 'why',
    front: 'Why must the automaton be deterministic **and** complete before you swap $A$?',
    back: 'With an NFA, swapping is wrong — a word can have both an accepting and a rejecting path. Without completeness, a dying run is neither accepted nor "in the complement", so the missing transitions must go to a dead state first.' },
  { id: 'c-cl-difference', concept: 'closure-props', kind: 'recall',
    front: 'Why is $L_1 \\setminus L_2$ regular?',
    back: 'Because $L_1 \\setminus L_2 = L_1 \\cap (\\Sigma^* \\setminus L_2)$, and regular languages are closed under both complement and intersection.' },

  /* ---------------- u3 : product ----------------------------------------- */
  { id: 'c-prod-def', concept: 'product', kind: 'recall',
    front: 'Give the product automaton for $L_1 \\cap L_2$.',
    back: '$F$ = $\\langle Q_1 \\times Q_2, \\Sigma, \\delta, \\langle q_1,q_2\\rangle, A_1 \\times A_2\\rangle$ with $\\delta(\\langle p_1,p_2\\rangle, c)$ = $\\langle \\delta_1(p_1,c), \\delta_2(p_2,c)\\rangle$.' },
  { id: 'c-prod-union', concept: 'product', kind: 'apply',
    front: 'How do you change the product automaton to accept $L_1 \\cup L_2$?',
    back: 'Keep everything and set $A$ = ($A_1 \\times Q_2$) $\\cup$ ($Q_1 \\times A_2$): accept if **either** component is accepting, instead of both.' },
  { id: 'c-prod-size', concept: 'product', kind: 'apply',
    front: 'DFAs with 3 and 4 states. How many states does the product have?',
    back: 'At most 12 = 3 $\\cdot$ 4, though usually far fewer are reachable. The construction is linear in neither input alone — it multiplies.' },
  { id: 'c-prod-why-complete', concept: 'product', kind: 'why',
    front: 'Why must both DFAs be complete before you build the product?',
    back: 'So that $\\delta(\\langle p_1,p_2\\rangle, c)$ = $\\langle \\delta_1(p_1,c), \\delta_2(p_2,c)\\rangle$ is defined for **every** pair and every character. If a component had a missing transition the pair would have no successor and $F$ would not be a DFA — completeness keeps the parallel run defined everywhere.' },
  { id: 'c-prod-practical', concept: 'product', kind: 'why',
    front: 'Why is there no practical "and" operator on regular expressions?',
    back: 'You would have to go regex $\\to$ NFA $\\to$ DFA $\\to$ product $\\to$ regex, and the resulting expression is usually absurdly large. Intersection is natural on automata, not on expressions.' },

  /* ---------------- u3 : emptiness --------------------------------------- */
  { id: 'c-empty-test', concept: 'emptiness', kind: 'recall',
    front: 'How do you decide $L(F) = $ {} for a DFA $F$?',
    back: 'Read $F$ as a directed graph, compute the set $R$ of states reachable from $q_0$, and check $R \\cap A$ = {}. Pure graph reachability.' },
  { id: 'c-empty-regex', concept: 'emptiness', kind: 'recall',
    front: 'Give the recursive emptiness test directly on a regular expression.',
    back: '$L(\\emptyset)$ = {}; $L(\\epsilon)$, $L(c)$ and $L(r^*)$ are never empty; $L(r_1 r_2)$ = {} iff **either** is empty; $L(r_1+r_2)$ = {} iff **both** are.' },
  { id: 'c-empty-equiv', concept: 'emptiness', kind: 'recall',
    front: 'How is equivalence of two regular expressions decided?',
    back: 'Build DFAs $F_{1,2}$ for $L(r_1)\\setminus L(r_2)$ and $F_{2,1}$ for $L(r_2)\\setminus L(r_1)$; then $r_1 \\doteq r_2$ iff **both** accept the empty language.' },
  { id: 'c-empty-why-both', concept: 'emptiness', kind: 'why',
    front: 'Why must you test **both** set differences?',
    back: 'Because $L_1 = L_2$ needs $L_1 \\subseteq L_2$ **and** $L_2 \\subseteq L_1$. One empty difference only proves one inclusion — e.g. $a$ versus $a+b$.' },
  { id: 'c-empty-decidable', concept: 'emptiness', kind: 'why',
    front: 'Why is regex equivalence decidable while grammar ambiguity is not?',
    back: 'Regular languages are closed under complement and intersection, so equivalence reduces to an **emptiness** test, which is mere reachability in a finite graph. Grammar ambiguity is instead equivalent to **Post\'s correspondence problem**, which is undecidable.' },

  /* ---------------- u3 : pumping ----------------------------------------- */
  { id: 'c-pump-statement', concept: 'pumping', kind: 'recall',
    front: 'State the Pumping Lemma exactly as in the notes.',
    back: 'If $L$ is regular there is $n \\in$ N such that every $s \\in L$ with $|s| \\geq n$ splits as $s = uvw$ with (1) $v \\neq \\lambda$, (2) $|uv| \\leq n$, (3) $\\forall h \\in$ N: $u v^{h} w \\in L$.' },
  { id: 'c-pump-n', concept: 'pumping', kind: 'why',
    front: 'Where does the number $n$ come from in the proof?',
    back: '$n$ := `card(Q)`, the number of states of a DFA for $L$. Reading $n$ characters visits $n+1$ states, so by pigeonhole one state repeats.' },
  { id: 'c-pump-vw', concept: 'pumping', kind: 'why',
    front: 'Why is $v \\neq \\lambda$, and why $|uv| \\leq n$?',
    back: 'The repeated states $q_k = q_l$ have $k < l$, so $v = c_{k+1}\\cdots c_l$ is non-empty; and the repetition is found within the first $n$ characters, so $l \\leq n$ gives $|uv| \\leq n$.' },
  { id: 'c-pump-h0', concept: 'pumping', kind: 'why',
    front: 'Why is $h = 0$ allowed, and what does $uv^{0}w$ mean?',
    back: '$h$ ranges over all of N including 0, so $u w$ (with $v$ deleted) must also be in $L$. Pumping **down** is often the quickest contradiction.' },
  { id: 'c-pump-direction', concept: 'pumping', kind: 'why',
    front: 'Does the Pumping Lemma prove that a language **is** regular?',
    back: 'Never. The notes state it only as: $L$ regular $\\Rightarrow$ $L$ can be pumped. It is used exclusively in contrapositive form, i.e.~to show a language is **not** regular.' },
  { id: 'c-pump-loop', concept: 'pumping', kind: 'why',
    front: 'What does the segment $v$ correspond to in the automaton?',
    back: 'A **cycle**: $q_k \\to^{v} q_k$. Traversing that cycle $h$ times keeps the run in the same state, hence $u v^{h} w$ is accepted for every $h$.' },

  /* ---------------- u3 : nonregular -------------------------------------- */
  { id: 'c-nonreg-game', concept: 'nonregular', kind: 'recall',
    front: 'Describe the non-regularity proof as an adversary game.',
    back: 'The adversary picks $n$; **you** pick $s \\in L$ with $|s| \\geq n$; the adversary picks the split $s = uvw$ obeying $v \\neq \\lambda$ and $|uv| \\leq n$; **you** pick $h$ that kicks $uv^{h}w$ out of $L$.' },
  { id: 'c-nonreg-anbn', concept: 'nonregular', kind: 'apply',
    front: 'Prove {$a^{k}b^{k}$ | k $\\in$ N} is not regular — which $s$ and which $h$?',
    back: 'Take $s$ = $a^{n}b^{n}$. Since $|uv| \\leq n$, $v = a^{k}$ with $k>0$. Take $h=0$: $uw$ has $n-k$ `a`s but $n$ `b`s, so $uw \\notin L$. Contradiction.' },
  { id: 'c-nonreg-choice', concept: 'nonregular', kind: 'why',
    front: 'Why must you choose $s$ but may not choose the split $u,v,w$?',
    back: 'The lemma says "**every** long word splits **somehow**". You get the existential choice of $s$ and $h$; the adversary gets the split. A proof that fixes the split proves nothing.' },
  { id: 'c-nonreg-balanced', concept: 'nonregular', kind: 'apply',
    front: 'Why is {w $\\in$ {a,b}$^*$ | count(w,a) = count(w,b)} not regular?',
    back: 'Pump $s = a^{n}b^{n}$ exactly as for $a^{k}b^{k}$: $|uv| \\leq n$ forces $v$ to be `a`s only, and $h=0$ destroys the balance.' },
  { id: 'c-nonreg-consequence', concept: 'nonregular', kind: 'why',
    front: 'What practical consequence does this have for programming languages?',
    back: 'Regular expressions cannot check balanced parentheses, so they cannot describe the syntax of a programming language. That is exactly why context-free grammars are introduced next.' },

  /* ---------------- u4 : cfg --------------------------------------------- */
  { id: 'c-cfg-tuple', concept: 'cfg', kind: 'recall',
    front: 'Give the 4-tuple of a **context-free grammar**.',
    back: '$G$ = $\\langle V, T, R, S\\rangle$: syntactic variables $V$, terminals (tokens) $T$ with $V \\cap T$ = $\\emptyset$, rules $R \\subseteq V \\times (V \\cup T)^*$, and start symbol $S \\in V$.' },
  { id: 'c-cfg-rule', concept: 'cfg', kind: 'recall',
    front: 'What exactly is a grammar **rule**, formally?',
    back: 'A pair $\\langle A, \\alpha \\rangle$ with $A \\in V$ and $\\alpha \\in (V \\cup T)^*$, written $A \\to \\alpha$. "Context-free" = the left-hand side is a **single** variable.' },
  { id: 'c-cfg-lang', concept: 'cfg', kind: 'recall',
    front: 'Define $L(G)$.',
    back: '$L(G)$ := {w $\\in T^*$ | $S \\Rightarrow^* w$} — only strings of **terminals**, only derivable from the start symbol.' },
  { id: 'c-cfg-conventions', concept: 'cfg', kind: 'recall',
    front: 'State the four notational conventions for writing grammars.',
    back: 'Start symbol = left side of the first rule; quoted symbols are terminals; names starting uppercase are terminals; names starting lowercase are variables.' },
  { id: 'c-cfg-parens', concept: 'cfg', kind: 'apply',
    front: 'Give a grammar for {$($^{n}$)$^{n} | n $\\in$ N}.',
    back: '$s \\to$ `(` $s$ `)` | $\\lambda$. The single variable remembers the nesting depth on the derivation stack — precisely what a finite automaton cannot do.' },

  /* ---------------- u4 : derivation -------------------------------------- */
  { id: 'c-der-step', concept: 'derivation', kind: 'recall',
    front: 'Define one derivation step $\\alpha b \\gamma \\Rightarrow_G \\alpha \\delta \\gamma$.',
    back: 'Replace **one occurrence** of a variable $b$ by the right-hand side $\\delta$ of some rule $b \\to \\delta$, leaving the context $\\alpha$, $\\gamma$ untouched.' },
  { id: 'c-der-star', concept: 'derivation', kind: 'recall',
    front: 'What do $\\Rightarrow^*$ and $\\Rightarrow^{n}$ mean?',
    back: '$\\Rightarrow^*$ is the reflexive-transitive closure (zero or more steps); $\\Rightarrow^{n}$ means exactly $n$ steps.' },
  { id: 'c-der-lr', concept: 'derivation', kind: 'recall',
    front: 'What are a **leftmost** and a **rightmost** derivation?',
    back: 'Leftmost always rewrites the leftmost variable, rightmost the rightmost. Top-down parsers build leftmost derivations; LR parsers build a rightmost derivation **in reverse**.' },
  { id: 'c-der-lr-name', concept: 'derivation', kind: 'why',
    front: 'What do the two letters in "LR parser" stand for?',
    back: 'L = the input is scanned **left to right**; R = the parser constructs a **reverse rightmost** derivation. Both halves come straight from the bottom-up picture.' },
  { id: 'c-der-count', concept: 'derivation', kind: 'apply',
    front: 'Does the *number* of derivation steps distinguish two parse trees?',
    back: 'No. Different orders of rewriting can give the same tree with the same step count. Ambiguity is about **trees**, not about derivation sequences.' },

  /* ---------------- u4 : parse-tree -------------------------------------- */
  { id: 'c-pt-def', concept: 'parse-tree', kind: 'recall',
    front: 'What labels may inner nodes and leaves of a parse tree carry?',
    back: 'Inner nodes: variables. Leaves: terminals **or** variables — a leaf may be a variable $a$ only if the grammar contains a rule $a \\to \\lambda$.' },
  { id: 'c-pt-children', concept: 'parse-tree', kind: 'recall',
    front: 'What must hold for an inner node labelled $a$ with children $X_1 \\ldots X_n$?',
    back: 'The grammar must contain the rule $a \\to X_1 X_2 \\cdots X_n$ — each inner node is exactly one rule application.' },
  { id: 'c-pt-fringe', concept: 'parse-tree', kind: 'recall',
    front: 'What do you get by reading the **leaf nodes** of a parse tree from left to right?',
    back: 'A word that is derived from the grammar $G$ — the string the tree structures. The tree is the derivation with the *order* of the steps forgotten.' },
  { id: 'c-pt-vs-deriv', concept: 'parse-tree', kind: 'why',
    front: 'Why is a parse tree a better object than a derivation?',
    back: 'Many derivations (leftmost, rightmost, mixed) correspond to **one** tree. The tree keeps only the structure, which is what the meaning depends on.' },

  /* ---------------- u4 : ambiguity --------------------------------------- */
  { id: 'c-amb-def', concept: 'ambiguity', kind: 'recall',
    front: 'When is a grammar **ambiguous**?',
    back: 'When some string of $L(G)$ has two structurally different parse trees. Two different *derivations* alone do not count.' },
  { id: 'c-amb-expr', concept: 'ambiguity', kind: 'apply',
    front: 'Why is $e \\to e$`+`$e$ | $e$`*`$e$ | N ambiguous?',
    back: '`1+2*3` parses both as `(1+2)*3` and as `1+(2*3)`; the grammar never says which operator binds tighter. The two trees evaluate differently.' },
  { id: 'c-amb-decidable', concept: 'ambiguity', kind: 'recall',
    front: 'Is ambiguity of a context-free grammar decidable?',
    back: 'No. The problem is equivalent to Post\'s correspondence problem, which is undecidable. A generator can only report the conflicts it happens to find.' },
  { id: 'c-amb-cost', concept: 'ambiguity', kind: 'why',
    front: 'What does ambiguity cost you in practice?',
    back: 'The parse tree — and hence the meaning — is not determined by the input. Since every LALR grammar is unambiguous, an ambiguous grammar **must** cause at least one shift-reduce or reduce-reduce conflict. (Conflicts are Chapter 11 material.)' },

  /* ---------------- u4 : precedence -------------------------------------- */
  { id: 'c-prec-layers', concept: 'precedence', kind: 'recall',
    front: 'How is operator precedence encoded into a grammar?',
    back: 'One variable per precedence level, each defined in terms of the variable of the **next higher** level: `arithExpr` $\\to$ `product` $\\to$ `factor`. The lowest-binding operator sits at the top.' },
  { id: 'c-prec-assoc', concept: 'precedence', kind: 'recall',
    front: 'How do left-, right- and non-associativity show up in the rule shape?',
    back: 'Left: $v_n \\to v_n\; o\; v_{n+1}$ (left recursive). Right: $v_n \\to v_{n+1}\; o\; v_n$ (right recursive). Non-associative: $v_n \\to v_{n+1}\; o\; v_{n+1}$ (no recursion at all).' },
  { id: 'c-prec-lark', concept: 'precedence', kind: 'recall',
    front: 'How does Lark resolve a shift-reduce conflict, and can you declare precedences?',
    back: 'There is **no** way to declare operator precedence in Lark. Every shift-reduce conflict is resolved in favour of **shifting**.' },
  { id: 'c-prec-1star2', concept: 'precedence', kind: 'why',
    front: 'Why does an un-layered Lark grammar parse `1*2+3` as `1*(2+3)`?',
    back: 'Shift always wins, so every operator behaves as if it had the same precedence and was **right** associative — a right-associating operator collects everything that follows it.' },
  { id: 'c-prec-power', concept: 'precedence', kind: 'apply',
    front: 'Write the rule for a right-associative power operator `^` at the `fact` level.',
    back: '`?factor : base "**" unary | base` — the right operand is the recursive one, so `x ** x ** x` is read as $x^{(x^{x})}$.' },

  /* ---------------- u4 : topdown ----------------------------------------- */
  { id: 'c-td-idea', concept: 'topdown', kind: 'recall',
    front: 'How does a recursive-descent parser work?',
    back: 'One function per syntactic variable. To parse $a \\to X_1 \\cdots X_n$ it calls the parsers for $X_1, \\ldots, X_n$ in order, splitting the input $w = w_1 \\cdots w_n$ with $w_i \\in L(X_i)$.' },
  { id: 'c-td-leftrec', concept: 'topdown', kind: 'why',
    front: 'Why does left recursion break a top-down parser?',
    back: 'A rule $a \\to a\\,\\beta$ makes the function for $a$ call itself with **no input consumed**: infinite recursion. Bottom-up parsers have no such problem.' },
  { id: 'c-td-fixes', concept: 'topdown', kind: 'recall',
    front: 'What two remedies does the lecture give for left recursion?',
    back: '(a) Rewrite the grammar so it is no longer left recursive; (b) move to an **EBNF** grammar, where the left recursion becomes iteration (a star) in a natural way.' },
  { id: 'c-td-ebnf', concept: 'topdown', kind: 'apply',
    front: 'Rewrite $v \\to \\lambda$ | $w\; v$ in EBNF, and why is the tree nicer?',
    back: '$v \\to w^*$. The parse tree becomes one node with a **list** of children instead of a right-leaning chain, so the transformer can just iterate.' },
  { id: 'c-td-direction', concept: 'topdown', kind: 'why',
    front: 'Which kind of derivation does a top-down parser produce?',
    back: 'A **leftmost** derivation, built forwards from the start symbol — the exact mirror image of the reverse-rightmost derivation an LR parser builds.' },

  /* ---------------- u5 : earley ------------------------------------------ */
  { id: 'c-ear-object', concept: 'earley', kind: 'recall',
    front: 'Definition: an **Earley object**.',
    back: 'A pair $\\langle a \\to \\beta \\bullet \\gamma, k \\rangle$ with $(a \\to \\beta\\gamma) \\in R$ and $k \\in$ {0,...,n}. $k$ records **where in the input** this attempt began.' },
  { id: 'c-ear-meaning', concept: 'earley', kind: 'recall',
    front: 'What does $\\langle a \\to \\beta \\bullet \\gamma, k\\rangle \\in Q_j$ mean?',
    back: 'The parser is trying to build an $a$ starting at position $k$, has matched $\\beta$ against $x_{k+1}\\cdots x_j$, and still needs to recognise $\\gamma$ from position $j+1$ on.' },
  { id: 'c-ear-ops', concept: 'earley', kind: 'recall',
    front: 'Name the three Earley operations and what each does.',
    back: '**Reading** (dot before a terminal $T$ with $x_{j+1} = T$: put $\\langle a \\to \\beta T \\bullet \\gamma, k\\rangle$ into $Q_{j+1}$); **Prediction** (dot before a variable $c$: add $\\langle c \\to \\bullet\\gamma, j\\rangle$ to $Q_j$); **Completion** (from $\\langle c \\to \\gamma\\bullet, j\\rangle \\in Q_i$ and $\\langle a \\to \\beta \\bullet c\\,\\delta, k\\rangle \\in Q_j$ add $\\langle a \\to \\beta c \\bullet \\delta, k\\rangle$ to $Q_i$).' },
  { id: 'c-ear-init', concept: 'earley', kind: 'recall',
    front: 'How are the Earley sets initialised, and what signals success?',
    back: 'Augment with $\\hat{s} \\to s$; set $Q_0$ = {$\\langle \\hat{s} \\to \\bullet s, 0\\rangle$} and all other $Q_i$ empty. Success iff $\\langle \\hat{s} \\to s \\bullet, 0\\rangle \\in Q_n$.' },
  { id: 'c-ear-lambda', concept: 'earley', kind: 'why',
    front: 'Why must prediction and completion be **iterated** within one $Q_i$?',
    back: 'With $\\lambda$-rules a freshly predicted item can complete immediately, which enables further completions. Only a fixed-point loop over both operations is correct.' },
  { id: 'c-ear-complexity', concept: 'earley', kind: 'recall',
    front: 'What are the complexities of Earley\'s algorithm?',
    back: '$O(n^{3})$ in general, $O(n^{2})$ for unambiguous grammars, and **linear** for LL(k) and LR(1) grammars.' },
  { id: 'c-ear-cyk', concept: 'earley', kind: 'recall',
    front: 'What is the complexity of the CYK algorithm, and what does it require of the grammar?',
    back: '$O(n^{3})$, and the grammar must first be converted into **Chomsky normal form**. Earley needs no such conversion, which is its practical advantage.' },
  { id: 'c-ear-closure', concept: 'earley', kind: 'why',
    front: 'Which LR notion is the Earley **prediction** operation identical to?',
    back: 'The `closure()` of a set of marked rules — the notes remark that computing `closure` **is** the Earley prediction operation.' },

  /* ---------------- u5 : shiftreduce ------------------------------------- */
  { id: 'c-sr-tuple', concept: 'shiftreduce', kind: 'recall',
    front: 'Give the 4-tuple of a shift-reduce parser.',
    back: '$P$ = $\\langle Q, q_0,$ `action`$,$ `goto`$\\rangle$ with `action`: $Q \\times T \\to$ `Action` and `goto`: $Q \\times V \\to Q$.' },
  { id: 'c-sr-actionset', concept: 'shiftreduce', kind: 'recall',
    front: 'What are the four kinds of element in the set `Action`?',
    back: '$\\langle$`shift`$, q\\rangle$ for $q \\in Q$; $\\langle$`reduce`$, r\\rangle$ for a rule $r \\in R$; `accept`; and `error`.' },
  { id: 'c-sr-config', concept: 'shiftreduce', kind: 'recall',
    front: 'What is a parser **configuration**, and which invariant links the two stacks?',
    back: 'A triple $\\langle$`States`, `Symbols`, `Tokens`$\\rangle$. Invariant: `length(States)` = `length(Symbols)` + 1, since the start state sits below the first symbol.' },
  { id: 'c-sr-reduce', concept: 'shiftreduce', kind: 'apply',
    front: 'On $\\langle$`reduce`$, a \\to X_{m-l}\\cdots X_m\\rangle$, what happens to the stacks?',
    back: 'Pop $l+1$ symbols and the same number of states, push $a$ onto `Symbols`, and push `goto`$(q_{m-l-1}, a)$ onto `States`. The input is **not** touched.' },
  { id: 'c-sr-eof', concept: 'shiftreduce', kind: 'why',
    front: 'Why is the special terminal EOF (`$`) added to $T$?',
    back: 'So that `action(q, $)` can be `accept`, and so that Follow sets have a token that legally ends a derivation. Without it the parser could not tell "done" from "more to come".' },

  /* ---------------- u5 : lr-items ---------------------------------------- */
  { id: 'c-item-def', concept: 'lr-items', kind: 'recall',
    front: 'Definition: a **marked rule** (an "item").',
    back: 'A triple $\\langle a, \\beta, \\gamma\\rangle$ with $(a \\to \\beta\\gamma) \\in R$, written $a \\to \\beta \\bullet \\gamma$: $\\beta$ has been seen, $\\gamma$ is still expected.' },
  { id: 'c-item-closure', concept: 'lr-items', kind: 'recall',
    front: 'Define `closure(M)` for a set $M$ of marked rules.',
    back: 'The smallest $K \\supseteq M$ such that $(a \\to \\beta \\bullet c\\,\\delta) \\in K$ and $(c \\to \\gamma) \\in R$ imply $(c \\to \\bullet\\,\\gamma) \\in K$. Fixed point over the variables just after the dot.' },
  { id: 'c-item-states', concept: 'lr-items', kind: 'recall',
    front: 'Which sets of marked rules qualify as **states**?',
    back: 'Exactly the closed ones: $Q$ := {M $\\subseteq \\Gamma$ | `closure(M)` = M}. An arbitrary set of items is not a state.' },
  { id: 'c-item-start', concept: 'lr-items', kind: 'recall',
    front: 'What is the start state $q_0$, and what is the augmented grammar?',
    back: '$\\hat{G}$ adds $\\hat{s}$ and the rule $\\hat{s} \\to s\\,$`$`. Then $q_0$ := `closure`({$\\hat{s} \\to \\bullet s\\,$`$`}).' },
  { id: 'c-item-goto', concept: 'lr-items', kind: 'recall',
    front: 'Define `goto(M, X)`.',
    back: '`goto(M,X)` := `closure`({$a \\to \\beta\\,X \\bullet \\delta$ | $(a \\to \\beta \\bullet X\\,\\delta) \\in M$}) — shift the dot over $X$ in every item that allows it, then close.' },
  { id: 'c-item-viable', concept: 'lr-items', kind: 'why',
    front: 'What does a state $M \\in Q$ of an SLR parser represent?',
    back: 'A **set of marked rules**: exactly those rules that can be applied in the situation described by the state. A completed rule $a \\to \\beta \\bullet$ in $M$ says the parser may reduce with $a \\to \\beta$.' },

  /* ---------------- u5 : follow ------------------------------------------ */
  { id: 'c-fol-first', concept: 'follow', kind: 'recall',
    front: 'Define `First(a)` for a variable $a$.',
    back: '`First(a)` := {t $\\in T$ | $\\exists \\gamma$: $a \\Rightarrow^* t\\,\\gamma$} — the tokens that can **start** a string derived from $a$.' },
  { id: 'c-fol-first-lambda', concept: 'follow', kind: 'recall',
    front: 'What is `First(lambda)` in these notes?',
    back: '`First`$(\\lambda)$ = {} — the **empty set**. $\\lambda$ is never an element of a First set here; emptiness is tracked separately by `nullable`.' },
  { id: 'c-fol-first-string', concept: 'follow', kind: 'recall',
    front: 'How is `First` extended to a string $a\\beta$?',
    back: '`First`$(a\\beta)$ = `First(a)` $\\cup$ `First`$(\\beta)$ if $a \\Rightarrow^* \\lambda$, otherwise just `First(a)`. And `First`$(t\\beta)$ = {t} for a terminal $t$.' },
  { id: 'c-fol-nullable', concept: 'follow', kind: 'recall',
    front: 'When is a variable `nullable` ($\\lambda$-generating)?',
    back: 'When $a \\Rightarrow^* \\lambda$. A rule $a \\to Y_1 \\cdots Y_k$ makes $a$ nullable if **all** $Y_i$ are nullable; $k=0$ is the special case $a \\to \\lambda$.' },
  { id: 'c-fol-def', concept: 'follow', kind: 'recall',
    front: 'Define `Follow(a)`.',
    back: '`Follow(a)` := {t $\\in T \\cup$ {`$`} | $\\hat{s} \\Rightarrow^* \\beta\\,a\\,t\\,\\gamma$} — tokens that can directly follow $a$ in some derivation from the **augmented** start symbol.' },
  { id: 'c-fol-dollar', concept: 'follow', kind: 'why',
    front: 'Why can `$` be in a Follow set but never $\\lambda$?',
    back: '`$` is a genuine terminal of the augmented grammar (from $\\hat{s} \\to s\\,$`$`). $\\lambda$ is not a terminal at all, so it cannot be an element of a set of tokens.' },
  { id: 'c-fol-rules', concept: 'follow', kind: 'apply',
    front: 'For a rule $a \\to Y_1 \\cdots Y_k$, when is `Follow(a)` $\\subseteq$ `Follow(Y_i)`?',
    back: 'When all of $Y_{i+1}, \\ldots, Y_k$ are nullable — in particular always for $i = k$. And `First`$(Y_{i+1}) \\subseteq$ `Follow`$(Y_i)$ unconditionally.' },
  { id: 'c-fol-fixpoint', concept: 'follow', kind: 'why',
    front: 'Why are First, Follow and nullable computed by a **fixed-point iteration**?',
    back: 'Their defining equations are mutually recursive. Start with all sets empty and `nullable` false, apply the rules until nothing changes; the sets only grow and are bounded, so it terminates.' },

  /* ---------------- u5 : slr -------------------------------------------- */
  { id: 'c-slr-shift', concept: 'slr', kind: 'recall',
    front: 'When is `action(M, t)` a **shift** in an SLR parser?',
    back: 'When $M$ contains $a \\to \\beta \\bullet t\\,\\delta$ with $t \\neq$ `$`. Then `action(M,t)` = $\\langle$`shift`, `goto(M,t)`$\\rangle$.' },
  { id: 'c-slr-reduce', concept: 'slr', kind: 'recall',
    front: 'When is `action(M, t)` a **reduce** in an SLR parser?',
    back: 'When $M$ contains a completed item $a \\to \\beta \\bullet$ with $a \\neq \\hat{s}$ and $t \\in$ `Follow(a)`. That Follow test is the whole of "S" in SLR.' },
  { id: 'c-slr-accept', concept: 'slr', kind: 'recall',
    front: 'When does the parser **accept**?',
    back: 'When $M$ contains $\\hat{s} \\to s \\bullet\\,$`$` and the next token is `$`. Everything not covered by shift, reduce or accept is `error`.' },
  { id: 'c-slr-emr', concept: 'slr', kind: 'recall',
    front: 'Definition: an **extended marked rule** (LR(1) item).',
    back: 'A quadruple $\\langle a, \\beta, \\gamma, L\\rangle$ with $(a \\to \\beta\\gamma) \\in R$ and $L \\subseteq T$, written $a \\to \\beta \\bullet \\gamma : L$. $L$ is the set of **follow tokens** carried with the item.' },
  { id: 'c-slr-emr-closure', concept: 'slr', kind: 'recall',
    front: 'How does closure work for extended marked rules?',
    back: 'From $a \\to \\beta \\bullet c\\,\\delta : L$ and $c \\to \\gamma$ add $c \\to \\bullet\\,\\gamma : \\bigcup${`First`$(\\delta t)$ | $t \\in L$} — i.e. `First`$(\\delta) \\cup L$ if $\\delta \\Rightarrow^* \\lambda$, else just `First`$(\\delta)$.' },
  { id: 'c-slr-lr-better', concept: 'slr', kind: 'why',
    front: 'Why is an LR(1) parser more powerful than an SLR parser?',
    back: 'SLR reduces on the **global** `Follow(a)`; LR(1) carries the lookahead of the **context in which the item was predicted**, which is usually a smaller set. Fewer spurious reduce entries, fewer conflicts.' },
  { id: 'c-slr-core', concept: 'slr', kind: 'recall',
    front: 'Define `core(M)`, and how are LALR states built?',
    back: '`core(M)` := {$a \\to \\beta \\bullet \\gamma$ | $(a \\to \\beta \\bullet \\gamma : L) \\in M$}. LALR merges all LR(1) states with the same core, taking the **union** of the follow-token sets rule by rule.' },
  { id: 'c-slr-goto-core', concept: 'slr', kind: 'why',
    front: 'Why is merging by core well-defined for `goto`?',
    back: 'Because `goto` ignores the follow tokens: `core(M_1)` = `core(M_2)` implies `core(goto(M_1,X))` = `core(goto(M_2,X))`. So `goto` is invariant under merging and `goto(M,X)` is the unique LALR state with the matching core.' },
  { id: 'c-slr-sizes', concept: 'slr', kind: 'apply',
    front: 'For the C grammar: how many states do the SLR, LR and LALR tables have?',
    back: '349 (SLR, but with conflicts — C is not an SLR language), 1572 (canonical LR), 350 (LALR). LALR buys LR-like power at SLR-like size.' },
  { id: 'c-slr-hierarchy', concept: 'slr', kind: 'recall',
    front: 'State the inclusion hierarchy of the three language classes.',
    back: 'SLR language $\\subsetneq$ LALR language $\\subsetneq$ canonical LR language. All three inclusions are **proper**.' },

  /* ---------------- u5 : conflicts --------------------------------------- */
  { id: 'c-con-sr', concept: 'conflicts', kind: 'recall',
    front: 'Define a **shift-reduce conflict** in an SLR parser.',
    back: '$M$ contains $a \\to \\beta \\bullet t\\,\\gamma$ (wants to shift $t$) **and** $c \\to \\delta \\bullet$ with $t \\in$ `Follow(c)` (wants to reduce). Both rules fire on the same cell.' },
  { id: 'c-con-rr', concept: 'conflicts', kind: 'recall',
    front: 'Define a **reduce-reduce conflict**.',
    back: '$M$ contains two different completed items $c_1 \\to \\gamma_1 \\bullet$ and $c_2 \\to \\gamma_2 \\bullet$ with $t \\in$ `Follow`$(c_1) \\cap$ `Follow`$(c_2)$.' },
  { id: 'c-con-lalr-sr', concept: 'conflicts', kind: 'why',
    front: 'Can merging LR(1) states into LALR states introduce a **shift-reduce** conflict?',
    back: 'No — never. A shift entry depends only on the core, which merging preserves; and a merged reduce lookahead was already a lookahead in one of the merged states, where the same shift existed. Only **reduce-reduce** conflicts can appear.' },
  { id: 'c-con-three-kinds', concept: 'conflicts', kind: 'recall',
    front: 'Name the three kinds of conflict distinguished by their origin.',
    back: '**Ambiguity conflicts** (the grammar is genuinely ambiguous); **look-ahead conflicts** (*reduce-reduce* conflicts in an unambiguous grammar where one token of look-ahead is not enough); **mysterious conflicts** (arise only when LR states with the same core are merged into LALR states).' },
  { id: 'c-con-mysterious', concept: 'conflicts', kind: 'recall',
    front: 'What exactly is a **mysterious reduce-reduce conflict**?',
    back: 'A reduce-reduce conflict that exists in the LALR automaton but not in the canonical LR automaton — created purely by merging two states with the same core. It means the language is LR but not LALR.' },
  { id: 'c-con-ambiguous-slr', concept: 'conflicts', kind: 'why',
    front: 'Is every ambiguous grammar non-SLR? Is every non-SLR grammar ambiguous?',
    back: 'Every SLR grammar is unambiguous, so an ambiguous grammar is never SLR. The converse fails: unambiguous grammars can still have SLR conflicts (that is what LR(1) fixes).' },
  { id: 'c-con-lark-debug', concept: 'conflicts', kind: 'recall',
    front: 'How do you make Lark tell you about its conflicts?',
    back: 'Raise the level of `lark.logger` (it is set to `CRITICAL` at import) **and** construct the parser with `debug=True`. With `strict=True` the first *shift-reduce* conflict raises a `GrammarError`; a *reduce-reduce* conflict always raises one, `strict` or not.' },

  /* ---------------- u6 : ast -------------------------------------------- */
  { id: 'c-ast-rules', concept: 'ast', kind: 'recall',
    front: 'State the three simplification rules that turn a parse tree into an AST.',
    back: '(1) If an inner node labelled $A$ has exactly one child that is a terminal $o$, drop the child and relabel the node $o$. (2) If an inner node has only one child, replace the node by that child. (3) Drop leaves labelled `(` or `)`.' },
  { id: 'c-ast-why', concept: 'ast', kind: 'why',
    front: 'Why does the rest of the compiler walk the AST instead of the parse tree?',
    back: 'The parse tree carries one node per precedence level and every punctuation character. The AST keeps only the operator/operand structure — exactly what evaluation and code generation need.' },
  { id: 'c-ast-lark-qmark', concept: 'ast', kind: 'recall',
    front: 'What do `?rule` and `-> alias` do in Lark, and can they create conflicts?',
    back: '`?rule` inlines a node that has exactly one child; `-> alias` renames the node of an alternative. Both affect only the **shape of the tree** — never the parse table, so they can neither introduce nor remove a conflict.' },
  { id: 'c-ast-alias-inline', concept: 'ast', kind: 'why',
    front: 'Why does `NUMBER -> number` survive although the rule is written `?atom`?',
    back: 'An alternative that carries an alias is **never** inlined, even with a single child. The alias is what the `Transformer` method hooks onto.' },

  /* ---------------- u6 : jasmin ----------------------------------------- */
  { id: 'c-jvm-memory', concept: 'jasmin', kind: 'recall',
    front: 'Into which four parts is JVM memory divided?',
    back: 'Program memory (the byte code), the **stack** (operands, arguments and local variables), the heap (objects), and the constant pool (constants and method addresses).' },
  { id: 'c-jvm-registers', concept: 'jasmin', kind: 'recall',
    front: 'What do the registers `SP` and `LV` point to?',
    back: '`SP` (stack pointer) points to the top of the stack; `LV` (local variables) points to the **first argument** of the current method. Locals are addressed as offsets from `LV`.' },
  { id: 'c-jvm-iadd', concept: 'jasmin', kind: 'apply',
    front: 'Stack (top last): $\\ldots$, a, b. What does `iadd` leave, and what does `isub` leave?',
    back: '`iadd` leaves $a+b$; `isub` leaves $a - b$ — the top value is subtracted **from** the one below it. Both pop two and push one.' },
  { id: 'c-jvm-loadstore', concept: 'jasmin', kind: 'recall',
    front: 'What do `iload v` and `istore v` do?',
    back: '`iload v` **pushes** the local variable with index $v$ onto the stack; `istore v` **pops** the top of the stack into local variable $v$. Indices start at 0.' },
  { id: 'c-jvm-branch', concept: 'jasmin', kind: 'recall',
    front: 'Contrast `ifeq l` with `if_icmpeq l`.',
    back: '`ifeq l` pops **one** value and jumps if it is zero. `if_icmpeq l` pops **two** and jumps if they are equal. Both remove their operands from the stack.' },
  { id: 'c-jvm-icmplt', concept: 'jasmin', kind: 'apply',
    front: 'Stack: $\\ldots$, a, b (b on top). When does `if_icmplt l` jump?',
    back: 'When $a < b$ — the value **below** the top is compared against the top. Both are popped either way.' },
  { id: 'c-jvm-limits', concept: 'jasmin', kind: 'recall',
    front: 'What do the directives `.limit locals` and `.limit stack` declare?',
    back: '`.limit locals` $l$: the size of the local variable frame — **parameters plus declared local variables** ($m+n$). `.limit stack` $s$: the maximal height the operand stack may reach. The JVM needs both in advance; limiting the stack height is a security feature against stack-overflow exploits.' },
  { id: 'c-jvm-invokestatic', concept: 'jasmin', kind: 'apply',
    front: 'Read `invokestatic Sum/sum(I)I`.',
    back: 'Call the static method `sum` in class `Sum`; it takes one `int` (the first `I`) and returns an `int` (the second `I`). The argument must be pushed first; on return it is replaced by the result.' },
  { id: 'c-jvm-ireturn', concept: 'jasmin', kind: 'recall',
    front: 'What is the difference between `ireturn` and `return`?',
    back: '`ireturn` returns the int value that is on top of the stack; `return` returns from a method that has no result. Pushing the value first is the caller-visible contract.' },
  { id: 'c-jvm-idiv', concept: 'jasmin', kind: 'why',
    front: 'Why is `idiv` not the same as Python\'s `//`?',
    back: 'Java rounds integer division **towards zero**, Python rounds **down**. So `-5/2` is $-2$ in Java but $-3$ in Python; likewise `-5 % 3` is $-2$ versus $1$.' },

  /* ---------------- u6 : codegen ---------------------------------------- */
  { id: 'c-cg-signature', concept: 'codegen', kind: 'recall',
    front: 'What does `compile_expr(expr, st, class_name)` return?',
    back: 'A pair: the list of Jasmin commands, and a natural number giving the **maximum stack growth** needed. The second component feeds `.limit stack`.' },
  { id: 'c-cg-st', concept: 'codegen', kind: 'recall',
    front: 'What is the **symbol table** `st` in this compiler?',
    back: 'A dictionary mapping each variable name to its index in the local variable frame, e.g. `st["x"] = 2`. That index is exactly the operand of `iload` / `istore`.' },
  { id: 'c-cg-var', concept: 'codegen', kind: 'apply',
    front: 'Compile the expression consisting of the single variable $v$.',
    back: '$\\langle$ [`iload st[v]`] $,\; 1 \\rangle$ — one instruction, and the stack grows by exactly one slot.' },
  { id: 'c-cg-binop', concept: 'codegen', kind: 'apply',
    front: 'Compile $lhs + rhs$ given $\\langle L_1,s_1\\rangle$ and $\\langle L_2,s_2\\rangle$.',
    back: '$\\langle L_1 + L_2 +$ [`iadd`]$,\; \\max(s_1, 1+s_2)\\rangle$. The $1+s_2$ is the key: while $rhs$ is evaluated, the value of $lhs$ is already sitting on the stack.' },
  { id: 'c-cg-maxstack', concept: 'codegen', kind: 'why',
    front: 'Why is the stack estimate $\\max(s_1, 1+s_2)$ and not $s_1 + s_2$?',
    back: 'The two sub-expressions are evaluated one after another, not simultaneously. Only the single finished value of $lhs$ survives into the evaluation of $rhs$ — hence the $+1$.' },
  { id: 'c-cg-bool', concept: 'codegen', kind: 'recall',
    front: 'What must the code for a Boolean expression leave on the stack?',
    back: 'The integer `1` for true or `0` for false. The JVM has no Boolean type at this level.' },
  { id: 'c-cg-equation', concept: 'codegen', kind: 'apply',
    front: 'Sketch the code for $lhs$ `==` $rhs$.',
    back: '$L_1$; $L_2$; `if_icmpeq true`; `bipush 0`; `goto next`; `true:`; `bipush 1`; `next:`. For `!=`, `<=`, `<` etc. only the comparison instruction changes.' },
  { id: 'c-cg-assign', concept: 'codegen', kind: 'apply',
    front: 'Compile the assignment $x$ `=` $expr$.',
    back: '$\\langle L\; +$ [`istore st[x]`]$,\; s\\rangle$ where $\\langle L,s\\rangle$ compiles $expr$. Evaluate, then store — and the net stack effect is zero.' },
  { id: 'c-cg-while', concept: 'codegen', kind: 'apply',
    front: 'Give the code skeleton for `while (cond) stmnt`.',
    back: '`loop:`; $L_1$ (the condition); `ifeq next`; $L_2$ (the body); `goto loop`; `next:`. Stack size $\\max(s_1,s_2)$.' },
  { id: 'c-cg-if', concept: 'codegen', kind: 'apply',
    front: 'Give the code skeleton for `if (b) s1 else s2`.',
    back: '$L_1$; `ifeq else`; $L_2$; `goto next`; `else:`; $L_3$; `next:`. Stack size $\\max(s_1,s_2,s_3)$.' },
  { id: 'c-cg-stmt-invariant', concept: 'codegen', kind: 'why',
    front: 'Which invariant must the code for a **statement** satisfy?',
    back: 'The stack height after the statement equals the height before it. It may grow and shrink in between, but the stack must be cleaned of all intermediate values once the statement has finished — that is what makes $\\max(s_1,\\ldots,s_n)$ a correct stack estimate for a block.' },
  { id: 'c-cg-labels', concept: 'codegen', kind: 'why',
    front: 'Why must every generated label be fresh?',
    back: 'Labels are method-global in Jasmin. Two nested `if`s reusing the label `next` would cross-jump into each other\'s code — hence a `new_label()` counter.' },
  { id: 'c-cg-structural', concept: 'codegen', kind: 'why',
    front: 'Why is code generation a **structural recursion** over the AST?',
    back: 'Each node type has a fixed code template that only needs the code of its children plus their stack sizes: $\\langle L_1,s_1\\rangle$, $\\langle L_2,s_2\\rangle$, $\\ldots$ are concatenated and the node\'s own instructions (`iadd`, `ifeq`, `goto`, labels) are inserted around them. Nothing else about the children is needed, so one recursive pass over the AST suffices.' },
];

export default cards;
