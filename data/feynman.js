/* Explain It Back — generation prompts.
   Producing an explanation in your own words is the single most reliable way to
   discover that you cannot actually explain it. Each prompt names an audience,
   because "explain X" and "explain X to someone who has not taken the course"
   are very different tasks, and only the second one exposes the gaps.

   `mustMention` is a self-check checklist, not a grader — matching a keyword
   proves nothing on its own, and the module says so. `model` is written to be
   read AFTER an attempt. */

export const prompts = [
  { id: 'f-alphabet', concept: 'alphabet',
    prompt: 'Your flatmate asks what a "formal language" is. Explain the chain alphabet → string → language, and say why the alphabet has to be finite.',
    mustMention: ['finite', 'subset', 'set of strings', 'Sigma'],
    model: 'An alphabet $\\Sigma$ is a finite, non-empty set of characters — think of it as the keys you are allowed to press. A string is a finite sequence of those characters, and $\\Sigma^*$ is the set of *all* such sequences, including the empty one, $\\lambda$. A formal language is then simply any subset of $\\Sigma^*$: a rule for which strings are "in" and which are "out". Nothing about meaning is involved — "language" here is a membership question and nothing else. The finiteness of $\\Sigma$ is load-bearing rather than decorative: a DFA needs a transition defined for every character out of every state, so with an infinite alphabet the transition table could not be written down at all. Note also the asymmetry that trips people up: $\\Sigma$ is finite and strings are finite, but a language may be infinite, and usually is. That gap — finite description, infinite object — is what the rest of the course is about.' },

  { id: 'f-star', concept: 'concat',
    prompt: 'Why does $L^*$ always contain the empty string $\\lambda$, even when $L = \\emptyset$? Explain it so that it sounds inevitable rather than like a convention.',
    mustMention: ['union', 'L^0', 'zero', 'empty string'],
    model: '$L^*$ is defined as the union of $L^n$ over all $n \\ge 0$, where $L^n$ means "concatenate $n$ strings drawn from $L$". The case $n = 0$ is concatenating *nothing at all*, and the result of concatenating no strings is the empty string $\\lambda$ — exactly as the product of no numbers is 1 and the sum of no numbers is 0. So $L^0 = \\{\\lambda\\}$ for every $L$ whatsoever, including $L = \\emptyset$, and since $L^*$ unions that case in, $\\lambda \\in L^*$ always. This is not a special-case patch bolted on to make the algebra work; it falls out of the definition. The check that convinces most people: $\\emptyset^* = \\{\\lambda\\}$, which is a one-element language, not the empty one. If you find that surprising, you are still reading $*$ as "repeat at least once" — that is $L^+$.' },

  { id: 'f-regex-sem', concept: 'regex-sem',
    prompt: 'Explain the difference between a regular expression and the language it denotes. Why does the course bother keeping $r$ and $L(r)$ notationally apart?',
    mustMention: ['syntax', 'semantics', 'finite', 'infinite', 'L(r)'],
    model: 'A regular expression is a piece of *syntax*: a finite string of symbols like `a(b+c)*`, built by the inductive rules. The language $L(r)$ is its *semantics*: an actual set of strings, very often infinite. $L(\\cdot)$ is the function that maps one to the other, defined by structural recursion — one clause per syntactic construct, each clause defined in terms of $L$ of the smaller parts. Keeping them apart matters because the interesting questions live in the gap. Two different expressions can denote the same language: `(a+b)*` and `(a*b*)*` are distinct syntax, identical semantics. If you conflate the two, the sentence "these regular expressions are equivalent" has no content — equivalence is precisely a statement about $L$, not about the text. It is the same distinction as between a program and the function it computes, and the whole chapter on algebraic simplification is about rewriting the syntax while provably preserving the semantics.' },

  { id: 'f-regex-algebra', concept: 'regex-algebra',
    prompt: 'A classmate writes $r^{**} = r^*$ and asks whether that is "just obviously true". Justify it properly, in terms of the languages.',
    mustMention: ['idempotent', 'L(r*)', 'closed under concatenation', 'both directions'],
    model: 'Prove it by mutual inclusion of the languages. One direction is free: $L(r^*) \\subseteq L(r^{**})$, because $L^* \\supseteq L$ for any $L$ (take $n = 1$), and here $L = L(r^*)$. The other direction is the substantial one. An element of $L(r^{**})$ is a concatenation of finitely many strings, each of which lies in $L(r^*)$, so each is itself a concatenation of finitely many strings from $L(r)$. Flatten the two levels and you have a single finite concatenation of strings from $L(r)$ — which is exactly the definition of a member of $L(r^*)$. So $L(r^{**}) \\subseteq L(r^*)$, and the two are equal. The general principle worth carrying away: $L^*$ is already closed under concatenation, so starring it again cannot reach anything new. "Obviously true" is the right instinct here, but the argument is what makes it a theorem, and the flattening step is where the actual content sits.' },

  { id: 'f-scanner', concept: 'scanner',
    prompt: 'Explain the maximal-munch rule to someone writing their first scanner. Why is it needed, and what breaks without it?',
    mustMention: ['longest match', 'ambiguous', 'priority', 'tie'],
    model: 'A scanner holds several token rules at once, and at any input position more than one can match — and each can match at several lengths. Something has to break the tie, or tokenisation is not a function. Maximal munch says: take the *longest* match available at the current position; if two rules tie at that same length, the one listed first wins. Without the length rule, `while` could be scanned as the identifier `w` followed by `hile`, or `==` as two separate `=` tokens — the scanner would be free to produce nonsense that the parser then has to reject for the wrong reason. Without the tie-break rule, a keyword and an identifier pattern both matching `while` at length 5 would leave the choice undetermined, which is why keyword rules are conventionally listed above the identifier rule. Both halves are needed: the first settles *how much* to consume, the second settles *which name* to give what was consumed.' },

  { id: 'f-dfa-nfa-power', concept: 'nfa',
    prompt: 'NFAs look strictly more powerful than DFAs — they can guess. Explain why they are not, without just naming the subset construction.',
    mustMention: ['set of states', 'finitely many', 'powerset', 'simulate', 'in parallel'],
    model: 'The guessing is real but it is not extra power, because you can simulate all the guesses at once. Instead of tracking which state the NFA is in, track the *set* of states it could be in after reading the input so far. That set updates deterministically: given the current set and the next character, the next set is completely determined — take every transition out of every member, then close under $\\epsilon$-moves. So "the set of possible states" behaves exactly like the state of a deterministic machine. The reason this terminates in a *finite* automaton is the crucial bit: the NFA has finitely many states, so it has only finitely many subsets — at most $2^n$ — and a machine whose states are those subsets is still a finite automaton. Non-determinism buys you convenience and sometimes exponentially smaller descriptions, but not a single extra language. The subset construction is just this argument written out as an algorithm.' },

  { id: 'f-eclosure', concept: 'eclosure',
    prompt: 'What is the $\\epsilon$-closure *for*? Explain why the subset construction takes closures at two separate moments.',
    mustMention: ['free moves', 'no input', 'start state', 'after each transition', 'fixed point'],
    model: '$\\epsilon$-transitions let the automaton change state without consuming input, so at any moment the machine is not really "in a state" — it is in every state reachable from there by free moves. The $\\epsilon$-closure of a set is exactly that: the set plus everything reachable by $\\epsilon$-edges alone, computed as a fixed point (keep adding until nothing new appears — it terminates because the state set is finite). The subset construction closes twice because there are two moments at which free moves can happen unobserved. First on the start state: before a single character is read, the NFA may already have drifted, so the DFA start state is the closure of $\\{q_0\\}$, not $\\{q_0\\}$. Second after every character: having moved on some symbol, the machine may again drift for free before the next character arrives, so each computed move-set is closed before it becomes a DFA state. Forget the first and your DFA may wrongly reject $\\lambda$; forget the second and it starts rejecting almost everything.' },

  { id: 'f-subset-terminates', concept: 'subset',
    prompt: 'Why is the subset construction guaranteed to terminate, and why is the resulting DFA sometimes exponentially larger?',
    mustMention: ['finitely many subsets', '2^n', 'worklist', 'reachable'],
    model: 'Termination is a counting argument. Each DFA state is a subset of the NFA\'s state set $Q$, and there are exactly $2^{|Q|}$ subsets — a finite number. The algorithm keeps a worklist and only ever adds a subset it has not produced before, so it can add at most $2^{|Q|}$ states; a loop that can only extend a bounded collection must stop. The same bound explains the blow-up: in the worst case the construction really does reach exponentially many distinct subsets, and there are families of NFAs (the classic being "the $n$-th symbol from the end is an `a`") where no smaller DFA exists at all — so this is a genuine lower bound, not a weakness of the algorithm. In practice it is usually far tamer, because only *reachable* subsets are ever built, and that is typically a small fraction of the powerset. That is exactly why the algorithm is written with a worklist rather than by enumerating all $2^{|Q|}$ subsets up front.' },

  { id: 'f-minimize', concept: 'minimize',
    prompt: 'Explain partition refinement as if describing a sorting process. What does it mean for two states to be "distinguishable"?',
    mustMention: ['equivalent', 'distinguishing string', 'split', 'accepting', 'fixed point'],
    model: 'Two states are *distinguishable* if some string $w$ leads from one into an accepting state and from the other into a non-accepting one — $w$ is then a witness that the two states cannot be merged, because the machine must behave differently from them. Minimisation looks for the opposite: states no string can tell apart. Start with the crudest possible guess, two blocks, accepting and non-accepting — these are distinguished by $\\lambda$ itself. Then repeatedly refine: if two states in the same block send some character into *different* blocks, that character plus whatever distinguished those blocks distinguishes them too, so split the block. Repeat until no split is possible. Termination is again finiteness — each round strictly increases the number of blocks, bounded by the number of states. The fixed point is the coarsest partition that respects the transition structure, and collapsing each block to a single state gives the unique minimal DFA. The useful mental image is sorting into ever-finer piles, where the only reason to split a pile is evidence that its members behave differently.' },

  { id: 'f-closure-complement', concept: 'closure-props',
    prompt: 'Explain why swapping accepting and non-accepting states gives the complement for a DFA but fails for an NFA.',
    mustMention: ['exactly one path', 'total', 'some path', 'both accept'],
    model: 'For a DFA the argument is immediate: on a given input there is exactly one computation, ending in exactly one state, and acceptance means that state is in $A$. Swapping $A$ for $Q \\setminus A$ therefore flips the verdict on every single string, which is precisely the complement. The argument leans on $\\delta$ being *total* — if the DFA has a missing transition and silently dies, that string is rejected by both the original and the swapped machine, so you must add the trap state before swapping. For an NFA it breaks because acceptance is existential: the NFA accepts if *some* path ends in $A$, and there can be many paths. A string may have one path into $A$ and another into $Q \\setminus A$, in which case it is accepted both before and after the swap — you get neither the language nor its complement. The fix is to determinise first: subset-construct, make the transition function total, and only then swap.' },

  { id: 'f-product', concept: 'product',
    prompt: 'Explain the product automaton, and why $\\cap$ and $\\cup$ differ only in one detail.',
    mustMention: ['lockstep', 'pairs', 'accepting set', 'both', 'either'],
    model: 'Take two DFAs for $L_1$ and $L_2$ and run them side by side on the same input. Track the pair of current states: the state space is $Q_1 \\times Q_2$, the start state is $(q_1, q_2)$, and the transition on character $c$ is componentwise, $\\delta((p,q), c) = (\\delta_1(p,c), \\delta_2(q,c))$. Because both machines are deterministic and total, this is again a perfectly good DFA and it simulates both originals simultaneously, in lockstep. Everything so far is independent of which operation you want — the entire difference is the choice of accepting set. For intersection, accept when *both* components are accepting: $A_1 \\times A_2$. For union, accept when *either* is: $(A_1 \\times Q_2) \\cup (Q_1 \\times A_2)$. Same states, same transitions, one different set. That is also a neat proof of closure under both operations for the price of one construction, and it is why intersection of regular languages is easy to *implement* even though it has no direct regular-expression operator.' },

  { id: 'f-emptiness', concept: 'emptiness',
    prompt: 'How do you decide whether two regular expressions denote the same language? Explain the reduction and why it terminates.',
    mustMention: ['symmetric difference', 'emptiness', 'reachability', 'witness'],
    model: 'Equality of sets is two inclusions, and $L_1 = L_2$ holds exactly when the symmetric difference $(L_1 \\setminus L_2) \\cup (L_2 \\setminus L_1)$ is empty. Regular languages are closed under complement and intersection, so that difference is itself regular and you can build an automaton for it: convert both expressions to NFAs, determinise, take the product, and set the accepting states to be the pairs where the two machines disagree. Now the question has become "is $L(A) = \\emptyset$?", and for an automaton that is plain graph reachability — is any accepting state reachable from the start state? A breadth-first search settles it in linear time in the size of the automaton, and it terminates because the automaton is finite. The part worth remembering for practice: when the answer is "not equal", BFS hands you a path to a reachable accepting state, and the labels along that path spell a shortest string on which the two expressions disagree. That concrete witness is far more useful than the verdict.' },

  { id: 'f-pumping-quantifiers', concept: 'pumping',
    prompt: 'State the pumping lemma and explain the quantifier alternation as a game: who chooses what, and in what order?',
    mustMention: ['adversary', 'you choose', 'n', 'uvw', 'h', 'order'],
    model: 'The lemma: if $L$ is regular then there is an $n$ such that every $s \\in L$ with $|s| \\ge n$ can be written $s = uvw$ with $|uv| \\leq n$, $v \\neq \\lambda$, and $uv^hw \\in L$ for all $h \\in \\N$. The alternation $\\exists n \\forall s \\exists (u,v,w) \\forall h$ is exactly a two-player game, and the order is everything. The opponent picks $n$ first — so your argument must work for *every* $n$, which is why $n$ stays a symbol and never becomes a number. You then pick $s$, and you may choose it as cleverly as you like, knowing $n$. The opponent then picks the split, adversarially, so you must beat *every* legal split, not the convenient one. Finally you pick $h$, and you only need one value that lands outside $L$. Almost every failed proof is an order violation: choosing $s$ before $n$, or beating a single split and declaring victory. The constraint $|uv| \\le n$ is your main lever, since it pins $v$ inside the first $n$ characters — which is why a good $s$ front-loads one symbol.' },

  { id: 'f-pumping-oneway', concept: 'nonregular',
    prompt: 'Your classmate pumps a language successfully and concludes it is regular. Explain, with an example, why that reasoning is invalid.',
    mustMention: ['necessary', 'not sufficient', 'one direction', 'contrapositive'],
    model: 'The lemma says regular $\\Rightarrow$ pumpable. Its useful form is the contrapositive: not pumpable $\\Rightarrow$ not regular. What it never says is pumpable $\\Rightarrow$ regular; that is the converse, and the converse is false. So succeeding at pumping tells you precisely nothing — you have failed to refute regularity, which is not the same as having established it. There are genuinely non-regular languages that pump perfectly well. A standard one is $L = \\{a^ib^jc^k \\mid i \\ge 1 \\Rightarrow j = k\\}$: any long enough word can be split so that pumping stays inside $L$, often by pumping in the region where $i = 0$ makes the condition vacuous, yet $L$ is not regular — the Myhill-Nerode theorem, or closure under intersection with $a b^* c^*$, shows it. If you actually want to *prove* a language regular, you must exhibit something: a regular expression, a DFA, or a construction from known regular languages by operations they are closed under. Pumping is a refutation tool only.' },

  { id: 'f-cfg-vs-regex', concept: 'cfg',
    prompt: 'What can a context-free grammar describe that a regular expression cannot, and what is the underlying reason?',
    mustMention: ['nesting', 'counting', 'stack', 'memory', 'a^n b^n'],
    model: 'The dividing line is unbounded nesting, and the canonical witness is $\\{a^nb^n\\}$ — matched pairs, arbitrarily deep. A finite automaton has only its current state as memory, and there are finitely many states, so after reading enough `a`s it must revisit a state and thereby forget how many it has seen; that is the pumping argument in one sentence. A grammar escapes this because a rule like $S \\to aSb$ is recursive: each application remembers one pair by *structure* rather than by state, and the derivation can nest as deeply as the input demands. Operationally the matching machine is a pushdown automaton — a finite control plus an unbounded stack — and the stack is exactly the memory a DFA lacks. This is why regular expressions cannot match balanced parentheses or nested comments, and why every real language needs a scanner *and* a parser: the scanner handles the flat, bounded-memory part (identifiers, numbers, operators) and the parser handles the nesting.' },

  { id: 'f-derivation-tree', concept: 'parse-tree',
    prompt: 'Explain the relationship between derivations and parse trees. Why do many derivations correspond to one tree?',
    mustMention: ['order', 'leftmost', 'same tree', 'independent', 'structure'],
    model: 'A derivation is a *sequence* of rewriting steps; a parse tree is the *structure* those steps build. Each step of a derivation expands one variable using one rule, which corresponds to giving one internal node its children. But when a sentential form contains several variables, the order in which you expand them is a free choice that does not affect the finished tree — expanding the left one first or the right one first attaches the same children to the same nodes, just in a different sequence. So a single tree corresponds to many derivations, one for each interleaving of independent expansions. To get a one-to-one correspondence you fix the order: a *leftmost* derivation always expands the leftmost variable, and each parse tree has exactly one of those (likewise exactly one rightmost). This is why ambiguity is defined in terms of trees or leftmost derivations, never derivations in general — otherwise every grammar with two variables in a right-hand side would look "ambiguous" for an entirely uninteresting reason.' },

  { id: 'f-ambiguity-cost', concept: 'ambiguity',
    prompt: 'What does ambiguity actually cost you when building a compiler? Use a concrete expression.',
    mustMention: ['meaning', 'tree', 'semantics', '2+3*4', 'parser cannot choose'],
    model: 'The parse tree is not decoration — it is what the rest of the compiler walks to decide what the program *means*. With the naive grammar $E \\to E + E \\mid E * E \\mid \\mathrm{NUMBER}$, the input `2+3*4` has two parse trees: one with `+` at the root, giving $2 + (3 \\cdot 4) = 14$, and one with `*` at the root, giving $(2+3) \\cdot 4 = 20$. Both are legitimate derivations, so the grammar itself does not say which program you wrote. A compiler cannot ship that: it must pick, and if the choice is made by an accident of the parser\'s implementation, the language has no specification worth the name. Practically it shows up as parser conflicts — an LR generator reports a shift-reduce conflict at exactly this point, because the table genuinely has two defensible entries. The fixes are to restructure the grammar into layers (expression / product / factor), which makes the intended tree the only derivable one, or to declare precedence and associativity so the generator resolves the conflict the way you intended.' },

  { id: 'f-precedence', concept: 'precedence',
    prompt: 'Explain how layering a grammar into expression/product/factor encodes precedence and associativity into the *shape* of the tree.',
    mustMention: ['lower in the tree', 'binds tighter', 'left recursion', 'associativity'],
    model: 'Each precedence level gets its own variable, and a level may only refer to itself or to the level below: $E \\to E + P \\mid P$, $P \\to P * F \\mid F$, $F \\to (E) \\mid \\mathrm{NUMBER}$. Because a `*` can only be introduced by $P$, and $P$ sits below $E$, a multiplication node can never end up above an addition node in the tree — so `*` is forced deeper, which is exactly what "binds tighter" means structurally. There is no separate precedence mechanism; the reachability of the grammar does it. Associativity comes from the *side* of the recursion. $E \\to E + P$ is left-recursive, so the recursive occurrence sits on the left and `1+2+3` must group as `(1+2)+3`, left-associatively. Writing $E \\to P + E$ instead would make it right-associative, which is what you want for exponentiation. The parenthesis rule $F \\to (E)$ is the escape hatch: it re-enters at the top level, letting the programmer override the ordering deliberately.' },

  { id: 'f-leftrecursion', concept: 'topdown',
    prompt: 'Why does left recursion break a recursive-descent parser, when it is perfectly fine for an LR parser?',
    mustMention: ['infinite', 'no input consumed', 'predict', 'stack', 'bottom-up'],
    model: 'Recursive descent is top-down: the function for $E$ decides what to do by looking at the upcoming token, then calls the functions for the symbols on the chosen right-hand side. With $E \\to E + P$, the very first thing `parseE` does is call `parseE` — with the input pointer unmoved. Nothing has been consumed, the state is identical, so it recurses forever and overflows the stack. The problem is structural, not a coding slip: the parser must commit to a rule *before* seeing the material that would justify it. LR parsing is bottom-up and has the opposite bias. It shifts tokens onto a stack and only reduces once a complete right-hand side is sitting there, so it can read `1`, reduce it to $E$, read `+`, read `2`, and only then apply $E \\to E + P$. The recursion is consumed rather than predicted, so left recursion is not merely tolerated but *preferred* — it keeps the parser stack shallow, whereas right recursion makes it grow with the length of the input.' },

  { id: 'f-first-follow', concept: 'follow',
    prompt: 'What are First and Follow sets for, and why must they be computed as fixed points rather than in one pass?',
    mustMention: ['decide', 'nullable', 'iterate until nothing changes', 'mutual recursion', 'lookahead'],
    model: 'Both answer lookahead questions. $\\mathrm{First}(\\alpha)$ is the set of terminals that can begin a string derived from $\\alpha$ — it tells a parser which rule could possibly apply given the next token. $\\mathrm{Follow}(A)$ is the set of terminals that can immediately follow $A$ in some sentential form, which is what an SLR parser consults to decide whether reducing to $A$ is legal right now. Neither can be computed in one pass because grammars are mutually recursive: $\\mathrm{First}(E)$ may depend on $\\mathrm{First}(P)$, which depends on $\\mathrm{First}(F)$, which may loop back. Worse, nullable variables propagate information backwards — if $B$ can derive $\\lambda$ then $\\mathrm{First}(BC)$ must also include $\\mathrm{First}(C)$, and $\\mathrm{Follow}(B)$ picks up $\\mathrm{Follow}(A)$ from $A \\to \\alpha B$. So you start with empty sets and apply the rules repeatedly until a full round adds nothing. The sets only grow and the terminal alphabet is finite, so the iteration reaches a least fixed point and stops. The classic slip these notes punish: a First set holds **only tokens** — $\\lambda$ is never a member, and indeed $\\mathrm{First}(\\lambda) = \\{\\}$. Whether a variable can vanish is tracked separately by the $\\mathrm{nullable}$ predicate, and Follow sets likewise range over the terminals plus the end marker $\\$$.' },

  { id: 'f-earley', concept: 'earley',
    prompt: 'Explain what a dotted item means in an Earley chart, and what predict, scan and complete each do.',
    mustMention: ['dot', 'progress', 'origin', 'predict', 'scan', 'complete'],
    model: 'A dotted item $A \\to \\alpha \\bullet \\beta$ with origin $j$ records a hypothesis in progress: "we started trying to build an $A$ at input position $j$, we have matched $\\alpha$ so far, and $\\beta$ is still outstanding". The dot is a progress marker and the origin is the bookmark that lets a finished item know where it began. Chart set $S_i$ holds every item consistent with the first $i$ tokens, and three operations close it. *Predict*: if the dot sits before a variable $B$, then every rule for $B$ might be starting here, so add $B \\to \\bullet \\gamma$ to $S_i$ with origin $i$. *Scan*: if the dot sits before the terminal that is actually the next token, advance the dot and put the result in $S_{i+1}$ — the only operation that moves forward in the input. *Complete*: if the dot has reached the end of $A \\to \\alpha\\bullet$ with origin $j$, then $A$ has been recognised spanning $j..i$, so go back to $S_j$ and advance the dot in every item that was waiting for an $A$. Repeat until nothing new appears; the input is in the language if $S_n$ contains a completed start rule with origin 0. The pay-off is that this handles *any* context-free grammar — ambiguous, left-recursive, whatever — because it pursues all hypotheses simultaneously instead of committing.' },

  { id: 'f-shiftreduce', concept: 'shiftreduce',
    prompt: 'Explain shift-reduce parsing to someone who has only seen recursive descent. What is on the stack, and what is a handle?',
    mustMention: ['stack', 'shift', 'reduce', 'handle', 'right-hand side', 'reverse'],
    model: 'The parser holds a stack and the remaining input, and has exactly two moves. *Shift* pushes the next input token onto the stack. *Reduce* pops the symbols matching some rule\'s right-hand side and pushes that rule\'s left-hand side in their place. Parsing succeeds when the input is exhausted and the stack holds just the start symbol. A *handle* is the occurrence on top of the stack that it is correct to reduce right now — not merely a right-hand side that happens to be sitting there, but one whose reduction is a step in a real derivation. Spotting handles is the entire difficulty, and it is what the LR table is for. The conceptual inversion from recursive descent is worth dwelling on: top-down parsing guesses a rule and then tries to fulfil it, whereas shift-reduce accumulates evidence and names it only once complete. What the parser produces is a rightmost derivation discovered *in reverse* — which is why the trees come out bottom-up and why left recursion causes no trouble at all.' },

  { id: 'f-lr-items', concept: 'lr-items',
    prompt: 'What does a state of the LR(0) automaton actually represent, and what is a viable prefix?',
    mustMention: ['set of items', 'simultaneously', 'closure', 'viable prefix', 'stack contents'],
    model: 'A state is a *set* of dotted items, and the set is the point: it records every rule the parser might currently be in the middle of, all at once, because with the evidence seen so far it genuinely cannot tell which. The closure operation is what makes the set honest — if some item has its dot before a variable $B$, then a $B$ might be starting here, so every rule for $B$ with the dot at the front belongs in this state too. Feeding a grammar symbol advances the dot in every item that was expecting it, and that gives the goto function. What the automaton recognises is the set of *viable prefixes*: the strings of symbols that can legitimately appear on the parser stack at some point in a successful parse — that is, prefixes of right-sentential forms that do not yet run past a handle. This is the deep fact that makes LR parsing work: the stack contents always form a viable prefix, the automaton run on the stack lands in a state, and that state lists exactly the possibilities still open. Because the DFA is finite, the parser can keep the state alongside each stack symbol instead of re-scanning the stack every step.' },

  { id: 'f-slr-vs-lr', concept: 'slr',
    prompt: 'Explain the difference between SLR, canonical LR(1) and LALR in terms of *when* each one is willing to reduce.',
    mustMention: ['Follow', 'lookahead', 'merge', 'cores', 'reduce-reduce'],
    model: 'All three build on the same item-set automaton; they differ only in how carefully they decide that a reduction is allowed. SLR is the crude version: seeing a completed item $A \\to \\alpha\\bullet$, it reduces whenever the lookahead lies in $\\mathrm{Follow}(A)$ — a set computed globally, ignoring which state you are in. That is often too generous, because a token can follow $A$ *somewhere* in the grammar while being impossible *here*, producing spurious conflicts. Canonical LR(1) fixes this by carrying a lookahead symbol inside each item, so the reduce decision uses exactly the tokens that can follow $A$ *in this context*. It is the most powerful of the three and its tables are the largest, since states now differ by lookahead as well as by core. LALR is the practical compromise used by yacc, bison and Lark: build the LR(1) states, then merge any two whose *cores* (the items ignoring lookahead) coincide, unioning their lookaheads. Tables shrink back to SLR size while keeping most of the precision. The exam-relevant subtlety: merging can introduce a reduce-reduce conflict, but never a shift-reduce one, because shift decisions depend only on the core, which merging leaves untouched.' },

  { id: 'f-conflicts', concept: 'conflicts',
    prompt: 'A parser generator reports a shift-reduce conflict. Explain what that means mechanically and what your options are.',
    mustMention: ['two actions', 'one cell', 'ambiguous', 'precedence', 'restructure', 'default'],
    model: 'It means that in some state, for some lookahead token, the construction derived two legal entries for one action-table cell: the completed item says "reduce", and another item with the dot before that token says "shift". The parser cannot do both, so something must decide. Mechanically it signals that one token of lookahead is not enough to tell the two derivations apart in this state. There are three honest responses. Restructure the grammar so the ambiguity disappears — layering an expression grammar into expression/product/factor is exactly this, and it is usually the right answer because it makes the intended tree the only derivable one. Or declare precedence and associativity, letting the generator resolve the cell deliberately; that is how the dangling-else is conventionally handled, and it is legitimate as long as you *chose* the resolution. Or accept the generator\'s default, which for shift-reduce is to shift — and this is where people get burned, because a silently defaulted conflict is a grammar bug that compiles cleanly and then mis-parses. A reduce-reduce conflict is more serious: the default is arbitrary and it nearly always indicates a real flaw in the grammar.' },

  { id: 'f-ast', concept: 'ast',
    prompt: 'Why does a compiler build an abstract syntax tree instead of just walking the parse tree?',
    mustMention: ['noise', 'parentheses', 'chain rules', 'structure', 'later phases'],
    model: 'The parse tree is a faithful record of how the parser worked, and that faithfulness is exactly the problem. It contains everything the *grammar* needed: nodes for parentheses that exist only to override precedence, single-child chains like $E \\to P \\to F \\to \\mathrm{NUMBER}$ that came from precedence layering, separators, and other tokens with no semantic content. None of that means anything to a type checker or a code generator; it is scaffolding left over from parsing. The AST keeps only what the *language* means: a `Plus` node with two children, a `Number` node with a value. Precedence is already encoded in the shape, so the parentheses can be discarded without losing information — the tree says what they said. The practical pay-off is that every later phase gets simpler and more stable: a code generator recurses over a handful of node types instead of navigating chain rules, and you can restructure the grammar to fix a parser conflict without touching any downstream phase, as long as the AST it produces is unchanged.' },

  { id: 'f-jasmin-stack', concept: 'jasmin',
    prompt: 'Explain why a stack machine makes code generation from an expression tree almost mechanical.',
    mustMention: ['postfix', 'post-order', 'operands', 'no registers', 'recursion'],
    model: 'On a stack machine every instruction takes its operands from the top of the stack and pushes its result back, so the code for an operation is simply: code for the left subtree, code for the right subtree, then the operator instruction. That is a post-order traversal of the expression tree written out linearly — the generated code is postfix notation, and postfix is just what you read off a tree in that order. So `compile(Plus(a, b))` is `compile(a); compile(b); iadd` and the recursion mirrors the tree exactly, with no bookkeeping in between. Correctness follows by induction: if each subtree leaves exactly one value on the stack, the operator finds precisely its two operands on top and leaves one behind, maintaining the invariant. The reason this is so much easier than a register machine is that there is nothing to allocate. A register machine forces you to choose *where* each intermediate value lives and to deal with running out of registers — register allocation and spilling — whereas the stack supplies an unbounded supply of anonymous temporaries in exactly the order the tree needs them. The cost is paid elsewhere: the JVM still needs `.limit stack`, so you must compute the maximum depth, which is another simple recursion over the same tree.' },

  { id: 'f-codegen-control', concept: 'codegen',
    prompt: 'Explain how a structured `while` loop becomes labels and jumps, and why fresh label names matter.',
    mustMention: ['label', 'conditional jump', 'back edge', 'unique', 'nested'],
    model: 'Assembler has no block structure, only labels and jumps, so a `while (c) b` is laid out as: a label $L_{start}$; the code for the condition $c$; a conditional jump to $L_{end}$ when it is false; the code for the body $b$; an unconditional jump back to $L_{start}$; then $L_{end}$. The back edge is what makes it a loop, and the test-at-the-top placement is what gives `while` its "may execute zero times" semantics — moving the test to the bottom would give you a `do-while` instead. Labels must be freshly generated, typically from a counter, because the construction is recursive and loops nest. If the generator emitted the fixed names `start` and `end`, an inner loop would reuse the outer loop\'s labels, the assembler would reject the duplicate definitions, and if it did not, the inner jump would silently target the outer loop. The same reasoning covers `if`, which needs its own `else` and `endif` labels. The rule of thumb: every syntactic construct that needs a jump target allocates its own names at the moment it is compiled, so two occurrences of the same construct can never collide.' },

  { id: 'f-whole-pipeline', concept: 'ast',
    prompt: 'Walk through what happens to the source text `x = 2 + 3 * y;` from characters to JVM instructions. Name each phase and what it hands on.',
    mustMention: ['scanner', 'tokens', 'parser', 'tree', 'code generation', 'stack'],
    model: 'The *scanner* reads characters and applies maximal munch to produce a token stream: `IDENT(x)`, `=`, `NUMBER(2)`, `+`, `NUMBER(3)`, `*`, `IDENT(y)`, `;`. Whitespace vanishes here, and this phase is regular — a finite automaton suffices, because nothing nests. The *parser* consumes those tokens against the grammar and produces a tree. Because the grammar is layered into expression/product/factor, the `*` node necessarily lands below the `+` node, so precedence is settled by structure rather than by a later fix-up; this phase needs a stack, which is why it is context-free and not regular. The tree is then trimmed to an *AST*: `Assign(x, Plus(Num 2, Times(Num 3, Var y)))`, with chain rules and punctuation discarded. *Code generation* recurses over the AST in post-order, emitting `bipush 2`, then for the product `bipush 3`, `iload y`, `imul`, then `iadd` to combine, then `istore x`. The result is postfix, which is simply the tree read bottom-up. Each phase narrows the representation — characters, then tokens, then structure, then meaning, then instructions — and each one hands the next a cleaner object than it received.' },
];

export const byConcept = c => prompts.filter(p => p.concept === c);
export default prompts;
